# 设计侧回复 · 第五轮（2026-09-24）

> 归档：设计侧交回的原文摘要（它项目 `ui/_incoming/24-设计侧回复（第五轮）.md`）。开发侧的处理见 `doc/00` §五十六。

交回六份：新屏 S12 目录视图、S13 Markdown 视图、S14 图片视图、S15 通用文件卡（不带 baseline）；改动 S1（底稿 aa84c3e63cc0c12d / 20260923-1431）、S9（底稿 72f0d7a3cf19e043 / 20260923-0255）。

- **第 3 题**：目录视图默认列表；非目录文件里图片 ≥ 60% 且 ≥ 6 张时自动切网格（`AUTO_MIN = 6`、`AUTO_SHARE = 0.6`），视图开关旁写「图片 8 / 9 · 自动网格」；手动切过就记住（提议 `layout.viewByDir: { [dir]: "list" | "grid" }`）。
- **S12**：六态（列表 / 网格 / 多选 / 空目录 / 只看稿件 = S1 / 图片目录自动网格）；无从属面板；目录在前其余按更新时间倒序；勾选框常驻（0.55 透明度）；网格非图片占位：稿件斜纹 + 文件名、图片棋盘格、md 摘录文字、其他大图标 + 扩展名；多选条贴底：已选 N 项 · 取消 Esc · 删除 · 移动… · 带进会话（实心）。
- **S13**：六态（渲染 / 源码 / 选区药丸 / 未落盘 / 快照弹层 / 窄窗大纲抽屉）；大纲按 S11 右侧一列；frontmatter 收成一行元数据可展开；未落盘齐边横条，落盘 ⌘S；快照弹层同 S2 形制，每行「回到这一版」。**要确认**：① `outline[].line` 是否计入 frontmatter 行；② 快照弹层需要 `snapshots: [{ version, src, at }]` 列表。
- **S14**：五态（适配 / 圈选中 / 圈完带备注 / 不支持图片 / svg）；无从属面板；缩放 25–400%，快捷键 0 / R / Esc；圈选真能拖，坐标用原图像素；不支持图片时说明常显。
- **S15**：三态（无引用 / 被稿件引用 / Web 版访达不可用）；居中卡片 ≤ 520px；`referencedBy[]` 建议 `{ file, line? }`；禁用项原因直接写在按钮下。
- **S1**：产品名换 Umbra Studio；项目下拉加「打开任意目录… ⌘O」（`onOpenDir`）；最近项目行下加类型统计 `{ dc, md, image, other }`（读 `LIVE.project.types`，缺就不显示）；新演示态 16「没有稿的目录」。
- **S9**：「已选中」药丸泛化五种 `node / range / region / files / dir`，键 `selections: [{ kind, label, detail }]`，可多颗、可全部清掉；态 7 去掉 emoji；新态 8。
- 自查：零 `class=`、SVG 几何属性无洞、图标走 IconGlyph、无 `input[type=number]`、控制流不进 table / select、控制台零条。
