# 给 ClaudeDesign 的改动清单

> ⚠️ **已被 `14-给 ClaudeDesign 的交办单.md` 取代（2026-09-20）。保留为往来记录，别再照着做。**
> 本文里 §三 的错误卡内容、§四之二 Q1 的 S7、§五 第 1–10 项均已完成；
> §二 整节的「已作废」结论**本身是错的**，以 §〇.1 为准（IconGlyph 判据恢复）。
> 仍然有效并已并入 14 的：§一 门控与增量改、§四 两条格式规则、§五 第 11 项。

> 这一份是**一次性交办单**。工具侧（UmbraDesign 的 MCP server + 本地 API）已经跑通，
> 现在卡在设计侧的几件事上。为了不让两边互相等，把需要你处理的**全部**列在这里。
>
> 读法：**§一 是最要紧的一条，请先看完再动任何文件。**
> §二～§四 是要改的；§五 是要你定形制的；§六 是改完怎么自查。
>
> 凡是标【实测】的都是在真浏览器里跑出来的读数，不是推断。

---

## 〇、交付回执（2026-09-18 · 工具侧追记）

`表单色板与布局方案.zip` 收到了，处置如下。

| 文件 | 处置 |
| --- | --- |
| `S7-属性面板.dc.html` | **落地为形制判据稿**。§五 第 1–8 项已按它重做进 S2，逐条见 `00` §27.3 |
| `IconGlyph.dc.html` | **落地**。S5 的图标网格已改引它，见下面那条纠错 |
| §五 第 9、10 项的结论 | 已实现（S3 顶栏版本位 + 「工作区」徽标；S5 「链没解开」露出下一跳） |
| §三 的错误卡内容 | 收到（meta 三行 / actions 两颗 / 不放 danger）。**还没写进 `任务.dc.html`** —— 那是你们仓库的稿，我不动，要我写就说一声 |
| S1–S5 这五份 | **丢弃，保工具侧的正本**。你们那五份和现有形制完全相同（差的全是接线与演示态门控），没有形制改动可移植 |

交付流程后面固定成这样，别再整包覆盖 `ui/`：
**原件原样丢进 `ui/_incoming/`**，我跑 `npm --prefix server run incoming`，
它逐份报「新文件 / 会覆盖 · 接线还在不在 · 合不合法 · renderVals 键少了哪些」。

### 〇.1 ⚠️ 一条撤回，撤回了 —— 对不起，这条害你们白改了一处

我上一轮把「`d="{{ }}"` 会让浏览器报 `Expected moveto`」当成**未复现**撤回了，
你们照那条把 S5 的图标网格改回了裸写 `d`，交付附言里也写了「§2.3 149 处作废」。

**撤回是错的。** 根因在我这边：`render_check` 里有一行静音，把
`attribute … Expected` 这一类控制台消息整段丢掉了 —— 那是我自己写的。
我拿那台被消音的仪器去做反向验证，量到零，就撤了一条真判据。

去掉静音重测，15 个形状，**100% 复现**：

| 会报 | 不会报 |
| --- | --- |
| `path d` · `polyline points` · `g transform` · `svg viewBox` | `fill` · `stroke-width`（涂装类宽容） |
| `circle cx` `r` · `svg width` · `rect x/y/w/h` · `line x1` | `img width` · `style` 里的洞 · `use href` |
| | `data-icon-d` + 静态 `d="M0 0"`，以及 `dc-import` 上的 `d` |

所以**你们建 `IconGlyph` 这个判断是对的，是我给的依据错了**。
`§2.3` 那条规则恢复：新稿的图标一律走 `IconGlyph`。
存量语料里这条现在是 158 处 / 14 份稿，其中 `path[d]` 110 处、
94 处集中在 `Umbra iOS 端` 与 `Umbra PC 端` 两份 ——
改法是那两份引 `IconGlyph`，不是逐处兜底。什么时候改由你们排，不急。

---

## 一、⚠️ 最要紧：`ui/S1`–`S5` 这五份稿**不要重新生成**，只能增量改

这五份稿现在**不只是设计稿了** —— 里面有工具侧接真实数据的接线。
重新生成一遍会把它们全冲掉，那是这两天工作量的大头。

### 1.1 每份稿里现在有什么

| 稿 | 加了什么 |
| --- | --- |
| `S1-稿件索引` | `LIVE` 门控；读 `window.__UD_INDEX`；顶栏吸顶高度实测；`go()` 跳 `S2-…?file=` |
| `S2-单稿预览壳` | `LIVE` 门控；本地 API（诊断/变更/定位/改属性/回退）；**注入点选桥**；属性面板；版本回退 |
| `S3-诊断面板` | `LIVE` 门控；诊断与体检读数来自本地 API |
| `S4-变更清单` | `LIVE` 门控；变更来自本地 API |
| `S5-设计系统浏览器` | `hasApi()` 门控；token / 图标 / 组件契约来自本地 API，**检索在服务端做** |

### 1.2 那条门控是什么，为什么这么设计

**一份文件两种用法，不分叉。** 在 `ui/` 下直接打开 → 没有真实数据，
走你自带的 9 个演示态，设计评审照旧；被工具部署进项目目录 → 显示真实数据，
演示态那一行整行不出现。

判据分两种，各有理由：

```js
// S1 / S2 / S3 / S4 —— 它们必须知道「看的是哪一份稿」
const LIVE = new URLSearchParams(location.search).get("file");

// S5 —— 它是项目级的（token / 图标 / 组件），不需要知道哪一份
const hasApi = () => !!(typeof window !== "undefined" && window.__UD_API);
```

工具在落盘时注入两个全局：

```js
window.__UD_INDEX = { project: {…}, drafts: [ … ] };   // 索引数据
window.__UD_API   = { base: "http://127.0.0.1:<port>/__ud/", token: "…" };
```

### 1.3 你改的时候请遵守

1. **保留所有 `LIVE` / `hasApi()` 分支**。要改样式就改样式，
   不要把 `LIVE ? 真数据 : 演示数据` 这种三元拆掉
2. **不要往散文里写死数字。** 这个坑我们踩了四次 ——
   上面三格显示「洞 3」而下面一句写着「143 个洞全部解析到了」，两句话自相矛盾，
   而且它**看起来像真的**，比明显的假数据更糟。
   规矩：给人看的文字里凡带数字或源码片段，必须和同屏数据同源
3. 改完请跑一遍自查（§六）

### 1.4 一个你可能想动但**动不了**的地方

样式字面量**没有实时预览**。拖滑块时中间态用不了运行时自带的
`window.__dcSetProps(name, overrides)` —— 那条只管 props，样式字面量没有覆盖通道。
所以现在是「松手才落盘、iframe 重载才看到」。

**这是机制缺口，不是形制问题。** 如果你觉得手感不能接受，需要设计一条样式覆盖通道
（比如往 iframe 注入一段临时 `<style>`），那是一个新功能，请在 §五 第 4 项里给意见。

---

## 二、⚠️ 这一节已作废：149 处改成 2 处

**如果你已经看过上一版的这一节，请整节丢掉。** 那一版让你批量改 149 处
（洞写在 SVG 的 `d` 等属性上），依据是我们在 S5 上观察到一条控制台 error。

**我们后来反向验证，复现不了，所以收回了这条判断。**

### 2.1 验了什么

| 试的东西 | 结果 |
| --- | --- |
| 语料里 4 份真稿（`PC 吐司` / `PC 折叠栏` / `PC 空态` / `PC 图标选择器`），都带 `d="{{ icon }}"` | 真渲染**零条** SVG 报警 |
| 8 种合成形态：裸写 `d` / `points` / `transform` / `viewBox` / `cx` / `svg[width]`，以及 `sc-for` 里 / 417 节点大页面 / 带 `hint-placeholder-count` / 外套 `sc-if false` | **全不报** |

只有 S5 那一次见过，触发条件没隔离出来。留着一条复现不了的判据让你改 150 多处，
等于制造「一条永远好不了的黄」—— 这是我们自己写下的不该做的事，所以撤。

`validate_draft` 已经不报 SVG 那一类了。`doc/06` §2.8 把它降成一条「见过一次、
复现不了」的备注：**不是规则，不用遵守**；万一 `render_check` 真报了这类控制台
error，改法是挂 `data-icon-d="{{ … }}"` + 静态占位 `d="M0 0"`，逻辑类里抄过去。

### 2.2 真要改的只剩 2 处

留下的是「浏览器解析时真会发请求」那一类 —— 这一类是实测过的：
`src="{{ previewSrc }}"` 每次加载留一个 404，改成 `about:blank` 之后消失。

| 稿 | 处 | 改法 |
| --- | --- | --- |
| `Umbra PC 端.dc.html` L169 | `iframe[src]` | 静态值写 `about:blank`，真地址在逻辑类里设（见下） |
| `Umbra iOS 端通用组件.dc.html` L94 | `iframe[src]` | 同上 |

```html
<!-- ✗ --> <iframe src="{{ url }}"></iframe>
<!-- ✓ --> <iframe ref="{{ frameRef }}" src="about:blank"></iframe>
```

```js
syncFrame() {
  const el = this.frameRef && this.frameRef.current;
  if (!el) return;
  const want = this.wantUrl;                      // 你这份稿里那个真地址
  if ((el.getAttribute("src") || "") !== want) el.setAttribute("src", want);
}
componentDidUpdate() { this.syncFrame(); }
```

范围（`validate_draft` 只报这些组合）：`img[src|srcset]`、
`script|iframe|source|audio|embed|track|input[src]`、`link[href]`、
`video[src|poster]`、`object[data]`。
**`<a href="{{ … }}">` 不算** —— 点击才跳转，解析时不发请求。

### 2.3 图标抽不抽组件：不急，但有一条实测结论给你

原来这一题是为了修那 149 处才提的，现在不用为它改了。不过我们把成本量了，
结论留着 —— **将来真要抽，不用担心渲染成本**：

| 形态 | 节点 | 耗时 |
| --- | --- | --- |
| 150 个 `dc-import` 引一个极简图标组件 | 465 | **1,426 ms** |
| 150 个内联 `<svg>` | 315 | **1,440 ms** |

组件方案多 150 个包装节点，**耗时反而略低**（差值在噪声内）。
所以「组件边界太贵」不成立。抽不抽你按组织方式定，**新稿**建议抽 —— 但这是建议，
不是必改项，也不阻塞任何事。

## 三、两个真缺陷（会静默失败，优先处理）

### 3.1 `任务.dc.html` 的错误卡拿不到 meta 与 actions

【实测】`PC 端/任务.dc.html` 与它的副本 `PC 端/Pages/任务.dc.html`，第 **291** 行：

```html
<dc-import name="PC 错误卡" … meta="{{ decideMeta }}" actions="{{ decideActions }}">
```

这两个洞在整份稿里**只出现这一次** —— `renderVals()` 里没有这两个键。
而 `PC 错误卡` 的 props 里它们是真的：

```
meta:    {label:string; value:string}[]
actions: {label:string; kind?:'primary'|'danger'|'ghost'; act?:()=>void}[]
```

所以那张「决策失败 · 模型调用被拒绝」错误卡**信息行是空的、一颗按钮都没有**，
而且**不报错、控制台干净**。属性洞解析不出时运行时会把整个属性丢掉，
一句告警都没有 —— 这是最安静的一类失败。

**要你定的是内容**：那张卡该显示哪几行信息（`meta`）、几颗什么按钮（`actions`）。
这是设计决定，我们不替你定。定了之后在 `renderVals()` 里补上这两个键即可。

### 3.2 29 个死键（值算出来也返回了，模板从不引用）

不影响渲染，属于死代码。抽查过 `collapsed` / `wsKeepOn` / `whyOpen` / `focused`
四个，全是真死键 —— 它们在逻辑类内部被用来算别的值，然后**多余地**返回了一次。

| 稿 | 死键 |
| --- | --- |
| `Umbra PC 端.dc.html` | `chevStyle` · `confirmApproved` · `confirmDenied` · `confirmPending` · `dynView` · `emptyState` · `goSettings` · `heroShown` · `heroStreaming` · `heroThinking` · `newSession` · `openLightbox` · `setMsgEl` · `showConvo` |
| `工作区.dc.html`（2 份副本） | `stop` · `wsKeepOn` · `wsWipeOn` |
| `PC 折叠栏.dc.html`（4 份副本） | `collapsed` |
| `图片消息与识图状态.dc.html` | `showWhy` · `whyOpen` |
| `提醒.dc.html`（2 份副本） | `noop` |
| `PC 图片预览器.dc.html` | `focused` |

> 顺带说一句：`S1` 与 `S2` 原稿里也各带一个 `demoLabel` 死键，已经替你删了。

---

## 四、两条新的格式规则（工具已经在查）

这两条都是实测踩出来的，写进了 `doc/06-写稿规则.md`：

### 4.1 §2.7 控制流标签不许落在 `table` / `select` 里 —— `E_CONTROL_IN_TABLE`（error）

`table` / `tbody` / `tr` / `select` / `optgroup` 只允许特定子元素。
HTML 解析器碰到**未知元素**（`sc-if` / `sc-for` / `dc-import` / `x-import`）
会把它**搬到外面去**（foster parenting），于是运行时在里面找不到它，
**一声不响地不渲染** —— 表头在、行是空的，没有报错，控制台干净。

**列表和表格都用 div + CSS grid。** 你的 `S1-稿件索引.dc.html` 全篇零个 `table`，
就是对的写法 —— 这条规则正是照它定的。

> 这条我们自己也踩过两次（工具生成的入口页、S2 的版本下拉），两次都是校验器抓的。

### 4.2 §2.8 解析时就动作的属性上不许写洞 —— `W_HOLE_IN_PARSED_ATTR`（warning）

见 §二。`style` / `title` / `class` / `aria-*` 这类解析时不动作的属性，写洞没问题。

### 4.3 不用管的：`W_HINT_IGNORED`（52 处）

`hint-*` 属性是**流式渲染**的配套，第一阶段没有流式，所以解析但忽略。
**存量的不用删**（语法仍然合法），新稿不要写就行。这一条不是缺陷。

---

## 四之二、你开工前那四个问题，我们的答复

### Q1 属性面板形制怎么交？ → **单独出一份 `S7-属性面板.dc.html`，只做形制不接数据**

不要在你那份 S2 里补。原因是 §一：我们这份 S2 里有 93 个 `renderVals` 键的接线、
点选桥注入、`componentDidMount` 的一串钩子 —— 两份 700 行带逻辑的稿做合并，
出错概率比重写还高。单独出一份，我们把**形制**移植过来，是单向搬运不是合并。

**文件名用 `S7`，不是 `S6`** —— `doc/08` 里编号已经占了：S6 是版本对比，S7 是 props 面板。

里面用你自己的演示数据就行，把这几种形态都给出来：数字项（带单位）、颜色项、
多值项（`margin: 0 0 8px`）、纯文本项、**不可改项**（例：`onClick="{{ onGo }}"`
要显示「这是函数（事件 / ref），不是可调的值」）、以及「在 sc-for 里 · 影响每一行」那条提示。

### Q2 那 27 份稿你改不了 → **先只改这五份壳，那批稿不用你管**

原来要你改那批稿是为了 §二那 149 处。**现在只剩 2 处**（见 §2.2），
我们自己改掉就行 —— 那两处在 `Umbra PC 端` 和 `Umbra iOS 端通用组件` 里，
我们有校验器和渲染体检可以逐份验证，你没有那条验证链。

所以：**不用把那批稿搬进你的项目。** 你只管这五份壳 + `S7`。

§三 那两件（`任务.dc.html` 的 `decideMeta` / `decideActions` 内容、29 个死键）
也在那批稿里 —— **但第一件要你定内容**（那张错误卡显示哪几行、几颗按钮），
定了之后告诉我们，我们去填。第二件（死键）我们自己删。

### Q3 图标走哪条 → **不用为这事改；新稿建议抽组件**

见 §2.3。成本量过了，抽组件不比内联慢。但既然 149 处已作废，这就不是必做项。

### Q4 样式字面量没有实时预览 → **通道我们已经建完了**

不需要你设计 —— 它是运行时机制不是形制。做法：往 iframe 注一张
`<style>`，按节点地址写 `[data-ud-node="…"]{ prop: value !important }`，
边敲边改这张表、**不落盘**；`Enter` / 失焦才真写文件；`Escape` 取消。

【实测】敲到 64px：iframe 显示 64px、文件版本停在 v7 不动、底栏显示
「预览中，未落盘：style.font-size → 64px」；回车后 v7 → v8，覆盖层撤掉。
`Escape` 取消：字号回到真值、覆盖层清空、之后失焦也不落盘。

⚠️ **只对 style 有效**，属性和文本没有。原因是所有权：样式表是 React 不管的
东西，写进去不会被下一次渲染冲掉；属性与文本由 React 托管，直接改会在重渲染时
被还原 —— 那种"预览"会闪回，比没有预览更糟。

**我们需要你的只有两件形态上的事**（在 `S7` 里体现就行）：

1. 「预览中，未落盘」这条提示长什么样（现在是底栏一行橙字）
2. **失焦要不要落盘** —— 现在会。也可以改成必须按一颗「应用」钮，
   那样更不容易误改，但多一次点击。你定

## 五、要你定形制的（工具侧已经能跑，先用了我们定的一版）

前 7 项在 `S2-单稿预览壳` 的右栏（属性面板，也就是 `08` 里的 S7 那块），
第 8 项在底栏。功能都通了，换皮不动逻辑。

| # | 项 | 现在这一版 | 要你定什么 |
| --- | --- | --- | --- |
| 1 | 属性面板整体形制 | 一行一项：标签 104px + 控件 + 下面一行灰色改法说明 | 分组（样式 / 属性 / 文案）、密度、是否折叠、不可改项怎么弱化。**交付形式见 §四之二 Q1：单独一份 `S7-属性面板.dc.html`** |
| 2 | 数字控件 | 文本框 + `−`/`+`，步长固定 1 | 要不要真滑块；步长怎么分（`px` 走 1、`%` 走 5？）；要不要能连着拖 |
| 3 | 颜色控件 | 17px 色块 + 文本框 | 要不要取色器；`var(--token)` 要不要接 S5 的 token 检索 |
| **4** | 样式字面量的实时预览 | **已经有了**（我们建完了，`00` §二十五）：边敲边改 iframe、不落盘；`Enter` / 失焦落盘；`Escape` 取消。底栏现在显示「预览中，未落盘：style.font-size → 64px」 | 只剩**形态**：那条「未落盘」提示长什么样（现在是底栏一行橙字）；以及**失焦要不要落盘** —— 现在会落盘，也可以改成必须按一颗「应用」钮 |
| 5 | `sc-for` 里的节点提示 | 顶上一条橙色胶囊「在 sc-for 里 · 影响每一行」 | 要不要更强的确认（改前弹一下？） |
| 6 | 落盘中的反馈 | 面板底下一行「正在落盘：…」 | loading 态怎么做、失败怎么呈现 |
| 7 | 不可改项的呈现 | 只显示值 + 一行改法说明，没有控件 | 例：`onClick="{{ onGo }}"` 显示「这是函数（事件 / ref），不是可调的值」 |
| 8 | 版本回退的形制 | 底栏 3 个版本胶囊 + 一颗「执行」 | 要不要版本列表 / 时间轴；要不要二次确认；超过 3 版怎么翻 |
| 9 | S3 顶栏的版本位 | 真数据时显示文件名，版本写「—」 | 版本该显示哪个（最新快照？工作区？） |
| 10 | S5 里 `resolved` 为 null | 显示「链没解开」 | `var(--x)` 指向另一个 `var(--y)` 时我们不跟链。要不要跟（那是另一个功能） |
| 11 | S1 的「索引已过期」横幅 | 橙色横幅摆在超限横幅上面，右侧列改过的文件名 | 形制我们照超限那条抄的，请过一眼。**这条本身不能省** —— build_index 之后改了稿，这一页的数字就全是旧的，而它看起来是新的 |

---

## 六、改完怎么自查

工具侧的判据是**零误报**，不是零告警。你改完之后：

1. **每一份动过的稿单独过一次** `validate_draft`，看 `W_HOLE_IN_PARSED_ATTR` 是否归零
2. **`render_check` 是唯一的验收证据** —— `validate_draft` 全绿不等于能渲染。
   看 `alive: true`、`nodeCount` 合理、**控制台零 error、断网零外部请求**
3. **改完稿要重新体检。** 体检读数带源码 sha256，稿动一个字就回到「未体检」——
   工具不会拿旧读数替新文件背书
4. 五份壳改完，请分别用两条路各看一次：带 `?file=` 和不带（S5 是有无本地 API）。
   两条路都要**控制台干净、零 404**

---

## 七、我们这边的状态（供你判断优先级）

- 工具：27 个 MCP 工具 + 本地 JSON API（`/__ud/*`），可读可写
- 三层修改：L3（Agent 直接沟通）与 L2（选中后告诉模型）已通；
  L1（人手动改属性）闭环已实测通过 —— 面板里改 `font-size`，iframe 里真的变了
- 存量 57 份稿：**零误报**，4 条 error 全是 §3.1 那个真缺陷
- **不阻塞你**：上面每一项我们都先用了一版能跑的，你改完直接换皮即可
