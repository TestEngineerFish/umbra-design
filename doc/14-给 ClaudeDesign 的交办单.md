# 给 ClaudeDesign 的交办单（2026-09-23 第四轮更新）

> **这一份是给你的唯一一份交办单。** 从 2026-09-21 起，**每一轮都随附一个文件包**
> （`UmbraStudio-ui-<时间>.zip`），请先读 §零。前两版（`doc/10`、`doc/13`）已经挪进
> `doc/_archive/`，不用去看 —— 它们里有相当一部分已经完成，还有两处自相矛盾。
> 以后所有交办都只更新这一份。
>
> 为什么重开一份：`doc/10` 是 09-17 写的，`doc/13` 是 09-20 02:12 写的，
> 而 02:20–09:52 之间 S6 / S8 / S9 / S10 与 S1 生命周期入口全部做完了（`doc/12` 已标 ✅）。
> 本文只写**现在还需要你做的**，已完成的都在 §〇 里划掉。
>
> 凡标【实测】的是真浏览器里跑出来的读数，不是推断。
>
> **2026-09-21 复核 + 修复**：上一版把 S6 / S9 / S10 说成「能跑的一版」，
> 实际它们渲染不出来。现在**已经修好并实测通过**（见 §二 开头），
> 你拿到的是三份真能打开、演示态能切的稿。
> 两条 AI 通道仍然「代码在但从未真跑过」，这一条没变，见 §七。

---

## 第四轮（2026-09-23 夜）：产品改名 Umbra Studio，新屏 S11–S15 + S1 / S9 改动

**先说变化**：产品从「PC 端设计软件」转成 **Umbra Studio 目录工作台** —— 打开一个目录，左对话、右预览，
按文件类型给不同的预览与编辑方式；`.dc.html` 仍是最深的一种类型，你交过的 S1–S10 **全部保留、全部有效**。
背景与依据在 `doc/18`，需求在 `doc/01` §4.1 / §4.2；**给你的委托全部在 `doc/08` §三之三**，这里只说流程。

**这一轮请你做什么**（按优先级）：

1. **S11 工作台布局壳**：五条布局规则 R1–R5 已定，请画六个状态；先回答 `08` §三之三末尾的第 1 题（从属面板放哪），这题定了 S12–S15 才好摆
2. **S12 目录视图**、**S13 Markdown 视图**、**S14 图片视图**、**S15 通用文件卡**：各按 `08` 给的数据形状与状态清单出形制
3. **S1 / S9 的改动**：S1 加「打开任意目录」与类型统计；S9 的「已选中」药丸泛化为五种

**流程不变**（§0.2）：随文档附 `npm run outgoing` 打的包（包里是 S1–S10 现版 + 皮肤 + 演示页），
新屏在包里的皮肤与 `IconGlyph` 上做；每份稿保留 baseline 行；交回放 `ui/_incoming/`。
**新屏没有底稿，baseline 行由我们收稿时补**，你不用管。

**两点提醒**：

- 应用前端正在用 React 重建（`12` M7），**你的稿是形制依据，不再直接当运行时嵌进应用**；所以状态切换的胶囊钮、假数据的键名照旧要对 —— 我们按键名接数据这条没变
- 皮肤 token 仍是 `--tool-` 前缀、浅深两份；产品名换了，**token 名不换**

**设计与开发并行**：开发先按 `11` §4.3 的 UI-1..UI-8 与 R1–R5 落地骨架；你的形制回来后我们并入再调。
所以**先交 S11 + 第 1 题的答复**，其余四屏可以晚几天。

**发包实况（2026-09-24）**：包 `UmbraStudio-ui-20260923-1431` 已在你项目的 `uploads/UmbraStudio-ui-20260923-1431/`，本文与 `doc/08` 也在 `uploads/`（文件名带「第四轮」），交办单 `uploads/21-交办单（2026-09-23 第四轮）.md`。
S2 那一份太大经 MCP 传不上，由 Sam 手动拖入覆盖；本轮不改 S2，不影响开工。触发句：「读 uploads/21-交办单（2026-09-23 第四轮）.md 照做」。

---

## 第四轮回执（2026-09-24）：换底稿 ✓、S11 ✓、第 1 题采纳

S11：`incoming` 新文件、零 blocking，已并入 `ui/`，六个演示态断网逐个点过，零洞、控制台零 error（`doc/00` §五十四）。
第 1 题：**采纳你的答案** —— 从属面板统一右侧一列（40 px 图标轨 + 300 px 面板体），底栏只留状态行；第 2 题的类型图标和「已选中」药丸位置一并采纳。`chatMode: "expanded" | "bar"` 与 `layout.panelByKind` 照你的键名。
你问的两件：① 第 17 个文件是 `README-给设计侧.md`，在 `ui/` 外面，你没数错。② S2 嵌入只留画布、属性面板归 S11 —— 方向同意，落地分两步（先把今天的 S2 面板装进 S11 的面板体里过渡，M7-7 再搬），**这一轮不改 S2**。
**下一步请画 S12–S15 和 S1 / S9 的改动**：按你定的右侧一列摆从属面板即可，不必等我们搬 S2。回执见它项目的 `uploads/23-开发侧回执（2026-09-24 第四轮 S11 已收）.md`。

## 第三轮回执（2026-09-23 晚）：已收，全部落地

S1 行内撤销：底稿正确、接线 4/4，按你给的键与两条 POST 接了 `delete_draft` / `restore_draft`，真浏览器里删除 → 塌行 → 撤销 → 文件回来。
会话栏裁决（在左 + 开关 + 列表可收起）：已做（`doc/12` M6-1）。这一轮没有欠项。回执见它项目的 `uploads/20-开发侧回执（2026-09-23 第三轮已收）.md`。

## 第三轮（2026-09-23 晚）：换底稿 + 两件小事

发件包 `UmbraDesign-ui-20260923-0255.zip`，已由开发 Agent 通过 MCP 放进它项目的 `uploads/UmbraDesign-ui-20260923-0255/`，
交办单同目录 `uploads/18-交办单（2026-09-23 第三轮）.md`。**触发句**（Sam 在它的 chat 里说）：
「读 uploads/18-交办单（2026-09-23 第三轮）.md 照做」。

要它做的：① 先用包整体替换 `ui/`；② S1 行内撤销（`justDeleted` 塌行）；③ 回一句会话栏放左还是放右（`doc/16` J1，不用改稿）。
**发包实况**：17 个文件里 16 个经 MCP `write_files` 内联写入并核过字节数（`support.js` 用它项目里的原件服务端复制）；
`react-dom.production.min.js`（132 KB ≈ 7.6 万 token）**超过单次调用输出上限，MCP 传不上去**，请 Sam 拖进 `uploads/UmbraDesign-ui-20260923-0255/ui/`。
经验：≥ 100 KB 的文件别走 MCP 内联；S2（120 KB）子代理转录后少 1 字节 —— 不影响底稿判定（`incoming` 比的是注释里记的 sha 与我们正本），但说明内联转录不保证字节精确。
不动：S2 / S6 / S8 / S9 / S10。

---

## 零之前、2026-09-23：第二轮已收，谢谢

你 2026-09-21 交回的六份（S1 / S2 / S6 / S8 / S9 / S10）我们通过 MCP 直接从你项目的 `ui/_incoming/` 取走了，
`incoming` 六份全部「底稿正确」、接线 4/4 · 8/8 一个不少 —— 协议第一次完整跑通。已并入 `ui/`，真浏览器逐屏看过。

三件要告诉你的：

1. **S8 少一个 `</div>`**：你把设置区包进两层容器（`max-width: 900px` 外框 + flex 面板），模板区末尾只补了一个闭合。
   浏览器会自动补齐所以画面没事，但我们的校验器按栈配平报 `E_TAG_UNBALANCED`。我们在本地补上了，
   **你那边的 `ui/S8` 还是缺的** —— 下次发包会覆盖回去，不用你动。
2. **`versionMeta` 已补**（你 §五 第 1 件）：`changes` 接口现在带 `versionMeta[v] = { src, time, summary }`，
   `src` 就你定的三个值。你的弹层一个字没改就把来源、时间、摘要显示出来了。
3. **两个 React UMD 已进发件包**（你 §五 第 2 件）：从下一个包起，`support.js` + 两个 React 文件和稿同层，直接打开不白屏。

另外接线时加了两个键：S1 `onRebuild` 现在真调重建并整页重载；S2 `onCommit` 接到了 `applyProp`。
你回复里说 `deleteDraft` 塌行需要 `s.justDeleted` 一类的键 —— 记下了，做 S1 行内撤销时一起加。

**这一轮没有新的交办。** 下次发包会带上并入后的现版（含上面的接线），到时再看。

---

## 零、⚠️ 先读这一节：这次随文档附了一个文件包（2026-09-21）

### 0.1 上一轮你卡住的原因，在我们这边

你在回复里说得完全对：**「那是你们那棵树，我这个项目里没有」**。

我们一直**只给你发文档，从没发过稿件文件**。你的项目在云端，只拥有被上传过的东西，于是：

- 我们新建的 S6 / S8 / S9 / S10，你一份都没有 —— §二 当然做不了
- 我们后来往 S1–S5 里加的接线，你那边也没有 —— 你手上的 S1 / S2 是**接线之前的老版本**，
  里面 `LIVE` 一次都没出现。所以你交回来的 S1 / S2 形制很好，但接线 4/4、8/8 全丢，
  S1 少了生命周期入口（27 个键），S2 少了整块属性面板（54 个键）

你写「`LIVE` 分支原样保留」是真心的 —— 你保留的是你那份里本来就没有的东西。
**你不可能知道自己没有什么**，这不是你的问题，是我们没把文件给你。这是第二次了，这次把协议补上。

### 0.2 协议（以后每一轮都这样）

**我们发**：`doc/14` + 一个文件包 `UmbraStudio-ui-<时间>.zip`（`npm run outgoing` 打出来的；
从 2026-09-22 起，可能由我们的开发 Agent 通过 MCP 直接放进你的项目，也可能由 Sam 转交，内容一样），
里面是我们 `ui/` 的**完整现版**：S1–S10 全部 11 份稿 + `IconGlyph` + 皮肤 + 演示页，外加一页说明。

**你收到后**：

1. **用这个包整体替换你项目里的同名文件**，以它为唯一底稿。你手上原有的 S1–S5 请直接丢掉
2. **每份稿 `<head>` 下第一行有一条 baseline 注释**，形如
   `<!-- umbradesign:baseline file="S1-稿件索引.dc.html" sha="09992b03…" sent="20260921-1456" … -->`。
   **请保留这一行。** 它记着「这份是哪一版」，交回来时我们靠它自动判断你是在哪一版上改的
3. **改完原样放进 `ui/_incoming/`**，连同一份回复（这次你已经这么做了，很好）

**我们收到后**：`npm run incoming` 第一行就会告诉我们「底稿正确 / 底稿过时 / 底稿不明」，
底稿不对就不往下看了 —— 不会再出现「形制很好但接线全丢」还要人工对照几百行的情况。

### 0.3 这一轮请你做什么

1. **把你这次的 §三 改动（索引过期横条、版本历史弹层、未落盘横条）在包里的 S1 / S2 上重做一遍。**
   你自己最清楚改了什么，在正确的底稿上重新落一次，比我们逐块翻译你的意图更忠实。
   你新增的键名（`hasStale` / `staleHeadline` / `versions[]` / `hasUnsaved` 等）照原样用，
   我们按你给的键名接数据
2. **§二 那四份：S6 / S8 / S9 / S10 现在都在包里了，演示态都能切。** 按 §二 的要求出形制
3. **§2.5 S1 生命周期入口摆位**：包里的 S1 已经有那段结构（新建项目 / 切换项目 /
   新建稿件 / 行内 改名·复制·移动·删除 / 回收站 / 项目设置入口），请在它上面给摆位建议

### 0.4 你上一轮已经定下来的，我们照单全收

这些是裁决，不需要你重做，**我们这边照着实现**（进度记在我们的待办里）：

| 项 | 你的裁决 |
| --- | --- |
| 两类横条的口径 | **齐边横条 = 这一屏的读数是暂时的；卡片 = 关于内容的一条提醒**。「索引已过期」「未落盘」用齐边横条，「元素数超限」用卡片 |
| 3.1 索引过期 | 齐边横条紧贴顶栏 + 一颗实心「重建索引」；**行级标记**：过期稿排最上、挂「文件已改」、三列读数灰掉（健康标签不灰） |
| 3.2 版本回退 | **不要底栏胶囊**。顶栏版本号可点 → 352px 弹层时间轴（版本号 · 来源：人手改 / AI / 新建 · 时间 · 摘要）；二次确认在行内，不用模态框；超过 3 版列表自滚动，不分页 |
| 3.3① 未落盘提示 | 顶栏下方齐边横条，药丸里写**真实的节点地址与属性名**，「落盘 Enter」「放弃 Esc」 |
| 3.3② 失焦落盘 | **保持现状，不加「应用」钮** —— 三重保险已够，应用钮让最高频的动作多一次点击 |
| UI-1 | 不加 16:9 预设；底栏两档（220 / 半屏）+ 拖拽把手，收起留 32px 条 |
| UI-2 | 稿件图标走 `IconGlyph`（页稿=文档、组件稿=六边形），不用中文字 |
| UI-3 | 健康四色不动；「渲染中」用 spinner + 上次健康色压到 55%，不进健康色 |
| UI-4 | **S6 独立窗口（或整页），不进底栏**；底栏给一颗「对比上一版」跳过去 |
| UI-5 | 搜索这一轮不扩，等 M5-1 全局搜索 |
| UI-6 | 顶栏加「新建稿件」，不加导出；到五颗收进 ⋯ 菜单 |
| UI-7 | 新建项目保持两步，但把 `prompt` 换成一张面板（能校验、能提示「这个目录里已有 12 份稿，要不要直接打开」）；不做模板选择 / Git 开关 |
| UI-8 | 做主题切换：浅 / 深 / 跟随系统，默认跟随系统，放 S8 项目设置 |

你的回复原文我们存档在 `doc/_archive/15-设计侧回复（2026-09-21）.md`。

---

## 〇、先说清楚：这几件**不用再做了**

`doc/10` 和 `doc/13` 里还写着要你做，实际已经交付并落地：

| 项 | 状态 | 出处 |
| --- | --- | --- |
| `S7-属性面板.dc.html`（节点级属性面板形制） | ✅ 你已交付，已落地进 S2，形制十条逐条采用 | `doc/10` §〇 |
| `IconGlyph` 子组件 | ✅ 已落地，S5 的图标网格已改引它 | `doc/10` §〇 |
| S3 顶栏版本位 · S5「链没解开」 | ✅ 你给的结论已实现 | `doc/10` §〇 |
| `任务.dc.html` 错误卡的 `meta` / `actions` **内容** | ✅ 已收到（meta 三行 / actions 两颗 / 不放 danger）。**剩下的是我们把它写进稿里，不是你的活** | `doc/10` §〇 |
| S1–S5 那五份副本 | 已丢弃，保工具侧正本（形制与现有完全相同，无可移植改动） | `doc/10` §〇 |

**另外有一条要更正**：`doc/10` §二 整节写着「149 处已作废」，而同一份文件的 §〇.1 说
**那次作废本身是错的**。以 §〇.1 为准 ——

根因在我们这边：`render_check` 里有一行静音，把 `attribute … Expected` 这一类控制台消息
整段丢掉了，我们拿被消音的仪器做反向验证，量到零就撤了一条真判据。去掉静音重测，
15 个形状 **100% 复现**。所以**你建 `IconGlyph` 这个判断是对的，是我们给的依据错了**。

规则恢复：**新稿的图标一律走 `IconGlyph`**（见 §五）。存量 158 处 / 14 份稿不急，什么时候改你排。

---

## 一、⚠️ 铁规矩：`ui/S1`–`S5` 只能增量改，不要重新生成

这五份稿**不只是设计稿** —— 里面有工具侧接真实数据的接线，重新生成会全冲掉。

⚠️ 适用范围：**S1–S5 全部，外加 S6–S10。**
新的五屏（S6 / S8 / S9 / S10）现在都有逻辑类与演示态，改样式请同样保留分支。

| 稿 | 里面加了什么 |
| --- | --- |
| `S1-稿件索引` | `LIVE` 门控；读 `window.__UD_INDEX`；顶栏吸顶高度实测；`go()` 跳 `S2-…?file=`；**生命周期入口（新）** |
| `S2-单稿预览壳` | `LIVE` 门控；本地 API（诊断/变更/定位/改属性/回退）；注入点选桥；属性面板；版本回退 |
| `S3-诊断面板` | `LIVE` 门控；诊断与体检读数来自本地 API |
| `S4-变更清单` | `LIVE` 门控；变更来自本地 API |
| `S5-设计系统浏览器` | `hasApi()` 门控；token / 图标 / 组件契约来自本地 API，检索在服务端做 |

**一份文件两种用法，不分叉**：在 `ui/` 下直接打开 → 没有真实数据，走你自带的 9 个演示态，
设计评审照旧；被工具部署进项目目录 → 显示真实数据，演示态那一行整行不出现。

```js
// S1 / S2 / S3 / S4 —— 它们必须知道「看的是哪一份稿」
const LIVE = new URLSearchParams(location.search).get("file");
// S5 —— 项目级的（token / 图标 / 组件），不需要知道哪一份
const hasApi = () => !!(typeof window !== "undefined" && window.__UD_API);
```

工具落盘时注入 `window.__UD_INDEX`（索引数据）与 `window.__UD_API`（`{base, token}`）。

**改的时候请遵守**：

1. **保留所有 `LIVE` / `hasApi()` 分支**。要改样式就改样式，别把 `LIVE ? 真数据 : 演示数据` 拆掉。
2. **不要往散文里写死数字。** 这个坑我们踩了四次 —— 上面三格显示「洞 3」而下面一句写着
   「143 个洞全部解析到了」，两句话自相矛盾，**而且它看起来像真的**，比明显的假数据更糟。
   规矩：给人看的文字里凡带数字或源码片段，必须和同屏数据同源。
3. 每份稿 ≤ 1,200 个静态元素标签；用 `ui/_ds-tool/tokens.css` 的 token。
4. 改完跑一遍自查（§六）。

---

## 二、五屏：**工具侧已经有第一版了，请你定形制**

这五件 `doc/13` 写的是「需要全新设计的屏」，那是 02:12 时的状态；02:20–02:38 我们各出了一版。

**2026-09-21 补一句经过**，因为它影响你怎么对待这五份：

上一版交办单发出前，S6 / S9 / S10 三份**其实渲染不出来** ——
既没引 `support.js`，逻辑那段的 `<script>` 也缺 `type="text/x-dc" data-dc-script`，
于是运行时根本不加载，98 / 38 / 49 个洞会原样印在画面上。
（我们的校验器当时把「没有逻辑类」当成「纯静态稿，没有洞要审」，整块洞审计被跳过，
三份稿报零 error 混了过去。判据已修，见 `doc/00` §三十。）

**现在都修好了，实测读数如下**（真 chromium，`render_check`）：

| 稿 | alive | 节点 | 未解析洞 | 404 | 外部请求 | 演示态切换 |
| --- | --- | --- | --- | --- | --- | --- |
| S6-版本对比 | ✓ | 149 | 0 | 0 | 0 | ✓ 逐个点过，内容真的变 |
| S9-会话面板 | ✓ | 144 | 0 | 0 | 0 | ✓ 同上 |
| S10-组件Props面板 | ✓ | 122 | 0 | 0 | 0 | ✓ 同上 |
| S8-项目设置 | ✓ | 115 | 0 | 0 | 0 | ✓ 同上 |

顺带被 `render_check` 抓出 S10 一条真告警：`<input type="number" value="{{ p.value }}">`
的 value 在解析时就按数字校验，控制台留一条 "cannot be parsed"。
已改 `type="text" inputmode="decimal"` —— S2 的属性面板一直是这么写的，原因就是这条。
这一条也补进了写稿规则，见 §5.2。

**所以对这五份的口径统一是**：我们先用了一版能跑的，**你看过之后给正式形制，我们换皮**。
五份都在 `ui/` 下，直接打开就能看演示态。§一 的铁规矩对全部五份都适用。

### 2.1 `S6-版本对比`（M4-1 ✅）

两版并排，滚动同步，变更处按四级着色（L1 契约 / L2 取值 / L3 文案 / L4 等价）。
和 S4 是同一份变更数据的另一种投影 —— S4 是清单，S6 是画面。

**现在这一版**：7 个演示态 —— 正常·四级变更都有 / 仅 L4 等价变更 / 仅 L1 契约变更 /
两版完全相同 / 跨 4 版合并对比 / 80+ 条变更 / 切换到清单视图。

**要你定**：并排布局的比例与分隔；滚动同步的交互（谁主谁从、能不能解锁）；四级高亮的配色与强度；
版本选择器的形态；「无差异」和「差异很多（80+）」这两个极端态怎么呈现。

### 2.2 `S8-项目设置`（M1-12 ✅）

从 S1 的项目名区域点进去。含危险操作区。

**现在这一版**：左侧 160px 吸顶 Tab 栏 + 右侧内容（最大宽 960px 居中），5 个演示态 ——
基本信息页 / 已配设计系统 / 限额设置 / 回收站有稿件 / 危险操作区。
内容覆盖：项目名称与标题、设计系统目录路径与别名、元素数 warning 阈值与 hard 上限、
回收站（可恢复 / 彻底删除）、重命名 / 移动目录 / 归档 / 删除项目（均二次确认）。

**要你定**：左 Tab 还是别的组织方式；危险操作区怎么和普通设置拉开距离（现在是一个区块）；
二次确认的形制（现在统一一种）；回收站是不是该独立成屏。

### 2.3 `S9-会话面板`（M2-7 ✅）

应用内 AI 会话，**嵌在预览壳侧边，不是另开窗口**。两条通道：provider 适配器（DeepSeek / 智谱）
与子进程 Claude Code（headless）。

**现在这一版**：6 个演示态 —— 正常的对话流程 / AI 正在流式输出 / 连续调用多个工具 /
运行中可中断 / 空会话 + 引导 / 选中节点后发给 AI。

**要你定**：消息流与工具调用怎么分层（现在工具调用是内联卡片）；流式输出的呈现；
中断按钮的位置与形态；用量（token / 花费 / 调用次数）摆哪；和 S2 预览壳并排时的宽度与可收起方式。

### 2.4 `S10-组件Props面板`（M4-2 ✅）

⚠️ **这不是 S7**。S7 是选中一个节点改它的样式 / 属性 / 文案；S10 是打开**组件稿**时调它的 props。
（`doc/08` 原来 S7 的定义指的就是这件事，已就地更正，见 `doc/11` D2。）

把 `data-props` 里 `editor` 非 `null` 的项渲染成控件，改了走运行时 `__dcSetProps` 重渲染：
`text` → 文本框 · `enum` → 下拉 / 单选 · `boolean` → 开关 · `number` → 数字控件 · `null` → 只显示值。

**现在这一版**：6 个演示态 —— 混合 editor 类型 / 全是 text / 全是 enum / 只有 null（回调）/
没有 props / 很多 props（20+）。

**要你定**：四种控件的形制（能不能和 S7 的数字 / 颜色控件共用一套）；`null` 项怎么弱化；
「没有 props」的空态；20+ 时要不要分组或搜索。

**一条规矩**：演示态切换钮**不要**放进 props 面板 —— 一个状态两个入口，点下去不知道哪个生效。

### 2.5 S1 生命周期入口（M1-11 ✅，不是新屏）

已经加进 `S1-稿件索引.dc.html`：新建项目 / 切换项目（最近项目下拉）/ 新建稿件 /
稿件行上的 改名·复制·移动·删除 / 回收站入口 / 项目设置入口（跳 S8）。后端全通。

**要你定**：这些入口在 S1 上怎么摆才不挤（顶栏项目名区 vs 行内操作菜单）；
行内操作是常驻还是 hover 出现；删除到回收站要不要提示。

---

## 三、现有屏上还欠的两条形制

### 3.1 S1 的「索引已过期」横幅

`doc/10` §五 第 11 项，也是 `doc/11` D4 点名**唯一仍在设计侧**的一条。

**现在这一版**：橙色横幅摆在超限横幅上面，右侧列改过的文件名 —— 形制照超限那条抄的。

**为什么不能省**：`build_index` 是一次性扫目录，之后改了稿它不知道，于是这一页的
元素数 / 健康 / 更新时间全是旧的，**而它看起来是新的**。请过一眼确认或给新形制。

### 3.2 版本回退的形制

**现在这一版**：底栏 3 个版本胶囊 + 一颗「执行」。

**要你定**：要不要版本列表 / 时间轴；要不要二次确认；超过 3 版怎么翻。

### 3.3 样式实时预览的两件形态事

通道我们已经建完了（不需要你设计机制）：往 iframe 注一张 `<style>`，按节点地址写
`[data-ud-node="…"]{ prop: value !important }`，边敲边改这张表、**不落盘**；`Enter` / 失焦才真写文件；
`Escape` 取消。【实测】敲到 64px：iframe 显示 64px、文件版本停在 v7 不动、底栏显示
「预览中，未落盘：style.font-size → 64px」；回车后 v7 → v8。

⚠️ **只对 style 有效**，属性和文本没有 —— 样式表 React 不管，写进去不会被下一次渲染冲掉；
属性与文本由 React 托管，直接改会在重渲染时还原，那种"预览"会闪回，比没有预览更糟。

**要你定两件**：① 「预览中，未落盘」这条提示长什么样（现在是底栏一行橙字）；
② **失焦要不要落盘** —— 现在会。也可以改成必须按一颗「应用」钮，更不容易误改，但多一次点击。

---

## 四、应用前端 M3-8：八条临时决策请你覆盖（**这一块之前没交过**）

这是 Umbra Studio 这个 PC 应用**自己的前端**（不是设计稿预览），`doc/11` §4.3。
前端当时没有设计稿，我们先按下面八条落地了，**每条都留了给你覆盖的口子**：

| # | 我们先定的 | 理由 | **要你定** |
| --- | --- | --- | --- |
| UI-1 | 三栏：侧栏 280px（可调 200–400）+ 主预览区 + 底栏 220px（可收起） | 设计软件标准布局，预览区要最大空间 | 比例要不要 16:9 预设；底栏要不要可调高度 |
| UI-2 | 稿件图标用文字缩写（「页」/「组」）而非 SVG | 简单，不依赖图标库 | 最终用哪种图标体系 |
| UI-3 | 健康标签四色：绿=通过 / 黄=有提醒 / 红=错误 / 灰=未体检 | 与 `.dc.html` 入口页契约一致 | 要不要更多状态（渲染中 / 构建中） |
| UI-4 | 底栏三个 tab：诊断 / 变更 / 稿件信息 | 对应 S3 / S4 的能力 | S6 版本对比放底栏还是独立窗口 |
| UI-5 | 搜索只搜稿名、文件名、演示态名 | 便宜的搜索；全局搜索是 M5-1 另一回事 | 搜索范围要不要扩到 token / CSS 变量 |
| UI-6 | 顶栏操作：重建索引 / 关闭项目 / 在浏览器打开 | 最常用的三个 | 要不要更多快捷操作（导出 / 建稿） |
| UI-7 | 新建项目两步：选目录 → 输名称 / 标题（prompt 对话框） | 最小流程，不额外加 UI 文件 | 要不要更丰富的新建向导（模板选择 / Git 开关） |
| UI-8 | CSS 变量 fallback 配色，引 `_ds-tool/tokens.css` | 与工具皮肤一致，断网可用 | 深色 / 浅色主题切换 |

---

## 五、写稿规则（新稿必须遵守；都是实测踩出来的，已写进 `doc/06`）

### 5.1 §2.7 控制流标签不许落在 `table` / `select` 里 —— `E_CONTROL_IN_TABLE`（error）

`table` / `tbody` / `tr` / `select` / `optgroup` 只允许特定子元素。HTML 解析器碰到未知元素
（`sc-if` / `sc-for` / `dc-import` / `x-import`）会把它**搬到外面去**（foster parenting），
于是运行时在里面找不到它，**一声不响地不渲染** —— 表头在、行是空的，控制台干净。

**列表和表格都用 div + CSS grid。** 你的 `S1-稿件索引.dc.html` 全篇零个 `table`，
就是对的写法 —— 这条规则正是照它定的。

### 5.2 §2.8 解析时就动作的属性上不许写洞 —— `W_HOLE_IN_PARSED_ATTR`（warning）

这一类浏览器解析时真会发请求：`src="{{ previewSrc }}"` 每次加载留一个 404。

```html
<!-- ✗ --> <iframe src="{{ url }}"></iframe>
<!-- ✓ --> <iframe ref="{{ frameRef }}" src="about:blank"></iframe>
```

范围（会发请求的一类）：`img[src|srcset]` · `script|iframe|source|audio|embed|track|input[src]` ·
`link[href]` · `video[src|poster]` · `object[data]`。
**`<a href="{{ … }}">` 不算** —— 点击才跳转。
`style` / `title` / `class` / `aria-*` 这类解析时不动作的属性，写洞没问题。

**另外两类解析时就按类型校验的**（不发请求，但控制台会留 error）：

1. **SVG 几何属性**：`path[d]` · `polyline|polygon[points]` · `g[transform]` ·
   `svg[viewBox|width|height]` · `circle[cx|cy|r]` · `ellipse[cx|cy|rx|ry]` ·
   `rect[x|y|width|height|rx|ry]` · `line[x1|y1|x2|y2]`。
   涂装类（`fill` / `stroke-width`）不算。修法见 §5.3 的 `IconGlyph`。
2. **`<input type="number">` 的 `value`**：解析时按数字校验，留一条
   "cannot be parsed, or is out of range"。
   修法：改 `type="text"`，要数字键盘就加 `inputmode="decimal"`。

【实测】这两类都是拿探针一个个量出来的，**不外推**：
`input` 的 `min` / `max` / `step` 不报，`type="range"` 不报（静默夹取），
`type="text"` 不报，`progress` / `meter` 的 `value` 不报。

### 5.3 新稿图标走 `IconGlyph`

见 §〇 的更正。成本我们量过：150 个 `dc-import` 引一个极简图标组件 = 465 节点 / **1,426 ms**；
150 个内联 `<svg>` = 315 节点 / **1,440 ms** —— 组件方案多 150 个包装节点，**耗时反而略低**。
所以「组件边界太贵」不成立。

### 5.4 不用管的：`W_HINT_IGNORED`

`hint-*` 是流式渲染的配套，第一阶段没有流式，所以解析但忽略。存量不用删（语法合法），新稿不写就行。
**这不是缺陷。**

---

## 六、交付方式与自查

**交付**：原件原样丢进 `ui/_incoming/`，我们跑 `npm --prefix server run incoming`，
它逐份报「新文件 / 会覆盖 · 接线还在不在 · 合不合法 · renderVals 键少了哪些」。
**别再整包覆盖 `ui/`。**

**自查**（我们的判据是零误报，不是零告警）：

1. 每一份动过的稿单独过一次 `validate_draft`，看 `W_HOLE_IN_PARSED_ATTR` 是否归零
2. **`render_check` 是唯一的验收证据** —— `validate_draft` 全绿不等于能渲染。
   看 `alive: true`、`nodeCount` 合理、**控制台零 error、断网零外部请求**
3. 改完稿要重新体检。体检读数带源码 sha256，稿动一个字就回到「未体检」——
   工具不会拿旧读数替新文件背书
4. 五份壳改完，分别用两条路各看一次：带 `?file=` 和不带（S5 是有无本地 API）。
   两条路都要**控制台干净、零 404**

---

## 七、我们这边的状态（供你判断优先级）

**2026-09-21 复核过一遍，下面是核实后的口径**（上一版把几处说满了）：

- 工具：27 个 MCP 工具 + 本地 JSON API（`/__ud/*`），可读可写 —— ✅ 实测
- 界面：**十屏全部能渲染、演示态都能切**（S6 / S9 / S10 于 2026-09-21 修好并实测）
- 三层修改：
  - **L1**（人手动改属性）—— ✅ 真浏览器实测闭环：改 `font-size`，
    iframe 的 `getComputedStyle` 真读到新值，落盘 v7 → v8，撤销能退回去
  - **L2 / L3** —— **数据链路**通了（改动落点、四级分级、回退都有回归证据），
    但**两条 AI 通道一次都没真跑过**：`agenttest` 是用 `patch_draft` 模拟 agent 的，
    不碰 `provider` / `channel_b`；机器上也没有任何 `ai_config.json`。
    所以「和 AI 对话改稿」目前是**未验证**的，不是「已通」
- 里程碑：M0 ✅ · M1 ✅（`lifecycletest` 全通）· M4 界面部分 ✅ ·
  M2 **代码在但未验证** · M3 外壳能跑 · M5 待排
- 存量 57 份稿：零误报 ✅
- **对你的影响**：十屏都是「换皮」，没有需要你从零做的。形制要求逐屏见 §二、§三。

---

## 附：工具侧对稿件的本地小补（下次发包会带上，你不用改）

- 2026-09-23 · S2：诊断行号判空 `d.line !== null` → `d.line != null`（两处）。信封约定定位不到就省略 `line`，原判法会显示 `Lundefined:undefined`。
- 2026-09-23 · S2 **嵌入模式**（`?embed=1`，应用把 S2 装进 iframe 时）：顶栏两行、底部状态栏、右栏标题与诊断 / 变更页签整块 `sc-if showHead` 掉，右栏只在选中节点后出现；`onMsg` 多认父窗口的 `umbradesign-app` 指令，`componentDidUpdate` 回报 `shell-state`。**独立打开不受影响。** 你改 S2 时请保留 `showHead` 那四处 `sc-if` 和这两段逻辑（`doc/00` §四十八）。
