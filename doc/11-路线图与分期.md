# Umbra Studio · 11 路线图与分期

> **这份文件只管两件事：什么时候做什么，以及已经定下来的决策。**
> 要什么看 `01-产品需求文档.md`；条目进度看 `12-待办清单.md`。
> 本文件**不重复**需求内容，只引用它的章节号。

---

## 一、分期总表

每一期以**「能不能真测」**为界，不以代码量为界。

| 期 | 名字 | 覆盖的需求 | 验收口径 |
| --- | --- | --- | --- |
| **M0** | 基础能力 | `01` §4.3（方式 ①）· §4.4 · §4.5 · §4.6 检索部分 | 已全部验收，读数见 `12` §一 |
| **M1** | 生命周期 | `01` §4.1 · §4.2 | `01` §7.5 第 18–20 条 |
| **M2** | 应用内 AI | `01` §4.7 · §4.3 的方式 ②③ | `01` §7.6 第 22–23 条 |
| **M3** | 应用外壳 | `01` §4.8 | `01` §7.7 第 24 条 |
| **M4** | 画布与对比 | `01` §4.3 剩余（props 面板 / 多宽度 / 深浅色）· §4.5 并排对比 | `12` §五 逐条 |
| **M5** | 打磨 | `01` §4.6 搜索与 token 编辑 · 并发保护 · 文档债 | `12` §六 逐条 |
| **M6** | 对照补齐 | `16` J1–J3 | `12` §六之二 逐条 |
| **M7** | 改名 + 前端重建 | `01` §4.1 · §4.9 · H7 | `01` §7.8 第 26–28 条 + 第 35 条；`selftest` / `rendertest` 全过 |
| **M8** | 第一批类型：目录 / `.md` / 图片 | `01` §4.2 第一批 · §4.8 泛型工具 · H3 / H5 | `01` §7.9 第 29–32 条 + 第 33 条 |
| **M9** | 壳与分发 | `01` §4.9 PC | `01` 第 36 条 |
| **M10** | 第二批类型 / 秘书接入 / Web 版 | `01` §4.2 第二批 · §4.8 · §4.9 Web · §九 | `01` 第 34 条 + `12` §六之六 逐条 |

**顺序的理由**：

- **M1 最先** —— 没有建项目和建稿，AI 会话没有可作用的对象。
  今天连「新建一份空白稿」都只能让模型写全文，那不是产品。
- **M2 在 M3 前** —— 三种编辑方式是产品的定义（`01` §三），外壳是包装。
  在浏览器里跑的 M2 已经可以每天用。
- **M3 最后再包装** —— 外壳需要先知道里面装什么；在它出现之前
  `npm run ui` + 浏览器已经是完整可用的形态，每期都能真测。

---

## 二、架构演进：每一期加了什么

### 2.0 今天（M0 完成后的状态）

```
┌─ 界面（浏览器）: S1 索引 · S2 预览壳+节点属性面板 · S3 诊断 · S4 变更 · S5 设计系统
│      ↕ 本地 JSON API（/__ud/*，随机令牌 + Origin 校验）
├─ 本地静态服务（127.0.0.1，随机端口）
├─ 核心层: 写入口 / 校验 / 渲染体检 / 语义快照与 diff / 节点地址 / 属性级写入 / 回退
└─ MCP server（stdio，27 个工具）—— 外部模型客户端从这里接
```

入口两条：`npm run ui`（人）· MCP 注册（外部模型）。

### 2.1 M1 加：生命周期能力层

核心层里新增一组操作（项目 / 稿件的增删改移 + 引用图谱），
**同时**暴露成 MCP 工具与本地 API —— 模型和界面用同一套。
新增一屏 S8 项目设置。

⚠️ 关键前置：引用图谱。`dc-import` 按文件名解析（`01` H4），
不先解决它，改名 / 移动 / 删除每一条都在埋雷。

### 2.2 M2 加：AI 会话层（两条大脑通道）

```
S9 会话面板 ──→ 会话层 ──┬─ 通道 A: provider 适配器（OpenAI Chat Completions 形状）
                        │     DeepSeek 按量 / 智谱通用 API 按量 / 其他兼容端点
                        └─ 通道 B: 子进程 Claude Code（headless）
                              模型指向 GLM Coding Plan 的 Anthropic 端点
                              —— 这样订阅额度才是合规使用（依据见 01 §六）
```

两条通道对上层是同一个接口；界面上只是「用哪个大脑」。
通道 A 是底线能力，通道 B 是增益（它依赖两个外部产品的行为）。

**会话层调的是我们已有的 27 个工具**，落盘仍走唯一写入口（`01` H3）。

### 2.3 M3 加：外壳

Tauri 壳 + 菜单 / 最近项目 / 文件夹对话框；
现有 server 作为 sidecar 由壳启停，端口与令牌交接给前端。
**这一期不该改核心层** —— 如果发现要改，说明前面某处把浏览器形态写死了。

### 2.4 M4 / M5

M4 只加界面（S6 对比、组件 props 面板、多宽度、深浅色、缩略图）。
M5 是跨稿搜索、token 编辑、并发写保护、文档债。

---

## 三、每一期的「可测试」是什么意思

不是「跑起来没报错」，是**有一条能复述的读数**，并且沉到 `fixtures/`：

| 期 | 回归 | 判据 |
| --- | --- | --- |
| M0 | `npm run selftest` / `rendertest` | 基准精确匹配 · 界面稿零 error · 语料零误报 |
| M1 | `npm run lifecycletest`（新） | 脚本化走完建→改名→移动→删→恢复，每步后 `E_IMPORT_MISSING` 为 0 |
| M2 | `npm run agenttest`（新） | 固定提问对固定稿，**不判措辞**，只判三件：改动落点 / 四级分级 / 能否回退 |
| M3 | 手工一遍 | 干净机器上走完首次使用流程 |
| M4 | 渲染基准扩充 | 并排对比的高亮位置与 S4 的行号对得上 |
| M5 | 各自 | 见 `12` §六 |

**M2 的判据要特别说一句**：AI 的输出不可复现，所以回归**不能判它说了什么**。
判据只能落在可验证的三件事上。这一条是为了避免把回归写成一个永远在飘的测试。

---

## 四、已定决策（Q1–Q10 · 2026-09-20 全部拍板完毕）

| # | 问题 | 结论 | 谁定的 |
| --- | --- | --- | --- |
| **Q1** | 分期顺序 | 生命周期 → AI → 外壳 → 画布 → 打磨 | 采纳建议 |
| **Q2** | AI 大脑 | **DeepSeek API 直连 + 智谱 GLM 订阅**。因订阅限官方工具，落成两条通道（`01` §六） | **用户指定** |
| **Q3** | 外壳技术 | **Tauri**。要的原生能力只有窗口 / 菜单 / 文件对话框 / sidecar | 采纳建议 |
| **Q4** | 删除语义 | **回收站**：移到 `.umbrastudio/trash/<时间戳>/`，界面给「恢复」，清空要二次确认 | 采纳建议 |
| **Q5** | 项目位置 | **任意路径 + 最近项目列表**；`projects/` 降级为默认新建位置 | 采纳建议 |
| **Q6** | 两个测试项目 | 留着当手工验收语料，**不进任何自动化判据**；随时可删，删了不影响任何回归 | 采纳建议 |
| **Q7** | 设计系统编辑 | M5 再做，**只改 token 取值，不做结构增删**，且改前给跨稿影响面 | 采纳建议 |
| **Q8** | 并发写 | M5 做写前版本校验：落盘前比对源码 sha256，不一致就拒绝并说清原因 | 采纳建议 |
| **Q9** | 文件夹分组 | **要**，M1 一起做；移动时引用跟着修 | 采纳建议 |
| **Q10** | `doc/01` 处置 | **整篇重写**，冲突内容删除不保留；需求文档可拆多份但**职责不许重合** | **用户指定** |

### 4.1 Q2 的落地细节（核实过的，不是推断）

- **DeepSeek**：OpenAI 兼容 `https://api.deepseek.com`，另有 Anthropic 兼容
  `https://api.deepseek.com/anthropic`；支持 tools 与流式；按量计费。
  **模型名由用户在配置里填，不硬编码** —— 各家迭代很快，写死迟早过期。
- **智谱 GLM Coding Plan**：官方文档明写「仅限在官方支持的指定工具与产品环境中使用」，
  且「在除规定工具外调用 API，不可享用 Coding 套餐的额度」。
  官方支持的工具里包含 Claude Code / OpenCode / OpenClaw 等。
  → 自研客户端直连**用不到订阅额度**，所以订阅只能走通道 B。
- **智谱通用 API（按量）**：走通道 A，和 DeepSeek 同一个适配器。

### 4.2 Q10 的落地：文档职责边界

需求相关的三份文件，职责互斥：

| 文件 | 只管 | **不管** |
| --- | --- | --- |
| `01-产品需求文档.md` | 要什么 · 硬约束 · 验收标准 · 不做什么 | 什么时候做、做到哪了、怎么实现 |
| `11-路线图与分期.md`（本文件） | 分期 · 架构演进 · 已定决策 | 需求细节（只引章节号）、条目状态 |
| `12-待办清单.md` | 条目 · 状态 · 验收方式 · 下一步 | 需求论证、决策理由 |

新增文档前先问一句：**它要管的事，现有哪份文件在管？**
重合就不要新建，去改那一份。

### 4.2.1 新登记的不确定项（2026-09-23，待用户拍板）

| # | 问题 | 现状 | 建议 |
| --- | --- | --- | --- |
| **Q11** | 通道 B 的端点配置 | ✅ 已落地（2026-09-23，`00` §34.2）：`ai_config.json` 有独立 `channelB`，`set_ai_config channel=b`，通道 B 不再回落到 A | 不需要拍板，登记为已做 |
| **Q12** | 应用前端里没有 AI 会话入口 | ✅ 2026-09-23 **用户拍板**：立项 `12` M2-12「S9 会话面板接进 Tauri 前端」 | 已登记，不再是不确定项 |

### 4.2.2 对照 ClaudeDesign 后的决策（2026-09-23，依据 `16`，**用户已拍板**）

| # | 问题 | 现状 | 建议 |
| --- | --- | --- | --- |
| **Q13** | 会话栏放左还是放右 | ✅ **用户指定**：做成开关，默认在左；稿件列表可收起 → `12` M6-1 | — |
| **Q14** | 要不要「评论」实体 | ✅ **用户指定**：做「钉在节点上的评论」：存 `.umbrastudio/comments.json`，S2 显示钉子，列表可标已处理，一键发给 AI；不做画框 → `12` M6-2 | — |
| **Q15** | 三样便宜的补齐 | ✅ **用户指定**：Code 只读视图 / Present 全屏 / 文字就地编辑都做 → `12` M6-3/4/5；画笔、Share、多人、PDF 仍不做（`01` §八） | — |
| **Q16** | 导入的稿没有节点地址，编辑壳里点选不到（`issues/2026-09-23/08`） | 节点地址只在落盘时打；用户自己的稿一个地址都没有，方式 ① ② 和评论对它全失效 | ✅ 定了（2026-09-24 用户）：**不做**。产品形态是目录工作台，「无论导入什么都能正常导入」是底线，导入这一步不做任何加工；**能不能点选 / 预览是打开它时那一层的事**，不支持的格式给一句提示即可。原列的三个选项都建立在「导入或打开时要动用户文件」上，前提被否 |
| **Q17** | 首页缩略图只有体检过的稿才有（`issues/2026-09-23/11`） | 截图来自体检记录；不点体检就没图 | ✅ 定了（2026-09-24 用户）：**暂时不要缩略图**，保持现状。首页靠名称 + 路径 + 稿数辨识够用；为「好看一点」让建索引多跑一次浏览器不值 |

### 4.2.3 转向 Umbra Studio 目录工作台的决策（2026-09-23，依据 `18`，**用户已全部拍板**：除 Q19 修正外全部采纳建议）

| # | 问题 | 现状 | 建议 |
| --- | --- | --- | --- |
| **Q18** | 产品定义要不要从「PC 端设计软件」改成「本地目录工作台，设计是它最深的一种文件类型」 | `01` §一 判据是「和 ClaudeDesign 基本一样」；参照物本身 2026-09-16 已并入对话、产物类型不限（`18` §2.1） | **采纳**，但守住「工作台不是通用编辑器」（`18` §三判断）；`01` §一 / §二 / §四 / §八 按 Q10 整段改 |
| **Q19** | 第一批加哪几种文件类型 | 类型矩阵见 `18` §4.2 | ✅ **用户指定**：第一批 = **`.dc.html`（已有，最初就是为设计做的）+ 目录 + `.md` + 图片**；代码 / 非 dc html 第二批（M10）；pdf / docx / xlsx 不排 |
| **Q20** | 布局策略 | R1–R5 见 `18` §4.3 | **按类型预设 + 可收起 + 记忆收放状态**，不做自由拖拽网格 |
| **Q21** | 前端工程化：动布局前是否引入构建（Vite，按类型拆模块） | `server/ui/index.html` 单文件 1,729 行 | **是**，作为 M7 的前置；不定就不动布局 |
| **Q22** | 非 `.dc.html` 文件的版本纪律 | 唯一写入口是 dc 专用链，别的文件走不了 | (a) 同样打快照 + 写 changelog · (b) 只 git 兜底 · (c) 写前 sha256 + 简单快照、无语义 diff。**建议 (c)**，界面上明示「这类文件只有快照」 |
| **Q23** | 秘书是否以此工作台作为它的文件面 | 现有 MCP 工具无泛型文件读写 | **是**；`list_files` / `read_file` / `write_file` / `move_file` 第一批就加 |
| **Q24** | 图片圈选 → AI 需要多模态，通道 A 能不能做 | DeepSeek 2026-08-21 已出 `deepseek-v4-flash-vision-exp`（OpenAI 兼容、支持工具）；智谱视觉模型同样兼容 | **能做**；配置里给每条通道加「支持图片」开关，模型名照旧由用户填 |
| **Q25** | 改名 Umbra Studio 后：重写还是在现有仓库上改 | 核心 `server/` 40 模块 + 回归基准是资产；前端是过渡产物（`18` §9.1） | **改**：核心留、前端重写、壳替换或保留、仓库改名，原仓库原历史 |
| **Q26** | 桌面壳：Tauri 保留还是换 Electron | 核心是 Node，Tauri 仍要捆 106 MB sidecar；三平台三种 webview | ✅ 采纳：**先 spike（`12` M9-1）再换 Electron**。**2026-09-24 spike 两条都过**（`00` §五十：自带 Chromium 跑体检读数与系统 Chrome 一致；核心在主进程里 MCP stdio 全程走通）→ **换 Electron，做 M9-2**；M9-3 随之成立。**2026-09-24 M9-2 / M9-3 已落地**（`00` §五十三），Tauri 目录已删 |
| **Q27** | Web 版：文件在哪、核心跑哪 | `18` §9.3 W1–W3 | 服务器工作区 + 核心跑 ECS 独立进程；Web 版排在 PC 之后 |
| **Q28** | 改名范围 | `18` §9.6 | 仓库 / 目录 / MCP 名 / `.umbrastudio` 目录全改，`.dc.html` 格式名不改 |
| **Q29** | 秘书接入形态 | `18` §9.5 | 第一期 MCP；第二期把 Studio 前端嵌进 UmbraPC 当面板 |
| **Q32** | 通道吃不吃图怎么定 | 按模型名猜一定有误判：`deepseek-chat` 文档没写多模态，**实测能看图**（`00` §六十一之一 有图 / 无图对照） | ✅ 定了：**现场探一次**（`probe_image_support`：随机纯色小图问颜色，答对才记 true）。模型名正则只当没探过时的默认，且往保守那边倒 |
| **Q33** | 多条通道的优先级与降级 | 用户 2026-09-24 加了通道 C（火山方舟 Agent Plan 订阅），要求优先用它、额度没了再用 A | ✅ 定了：`defaultChannel` 决定默认走哪条；C 报**额度类**错误（`looksLikeQuotaProblem`）**且这一轮没调过工具**时自动换 A 重跑一次，会话里记一条系统消息。两个条件缺一不可 —— 只认额度类错误免得把真问题藏起来；只在没调过工具时降级免得重复改稿（`00` §六十二） |
| **Q34** | 通道 B 到底接谁 | 用户 2026-09-24 想用已付费的 Cursor Pro，把 Cursor 的 key 填进了 `channelB`。**实测 Cursor 接不进来**：它的 Cloud Agent API 是 agent 编排（在 GitHub 仓库上派云端 agent），`POST /v1/chat/completions` 与 `POST /v1/messages` 都是 404（`00` §六十四）。它给的是「我帮你跑完整个 agent」，我们要的是「给我一个模型，agent 循环我自己跑」，形状根本不同 | ✅ 定了：`channelB` 的 `baseUrl` / `apiKey` **留空 = 用本机已登录的 Claude Code**（`claude` 子进程不覆盖 `ANTHROPIC_*`，走用户自己的订阅，不额外花钱）；两个都填 = 指向别家 Anthropic 兼容端点（如 GLM Coding Plan），这时才做端点预检。界面上用 `via: local / endpoint` 把两种分开 —— 分不清就会再填错一次 |
| **Q35** | 本地 CLI 要不要做成一类通道 | 用户 2026-09-24：项目设置里能选本地 CLI（Claude / Codex / Cursor…），并能扫环境。调研结论：**走得通且比接 API 划算** —— 这些 CLI 走登录态而非 API key，用户已付的订阅能直接用，而且都能加载 MCP server（用上我们 66 件工具）。这也修正了 Q34 的一半：Cursor 的 **API** 接不进来，**CLI** 完全可以（`00` §六十五） | ✅ 定了：通道 B 就是「本地 CLI」这一类，不新开通道。`channelB.cli` 选哪个（缺省 `claude`，老配置照旧）。五家能力不齐，**界面照实标三件事**：没实测过的标出来、要不要往项目写配置文件、报不报用量。`verified` 只在真机跑通过才是 true。配置**存全局**（CLI 与登录态属于机器，Q7 同理），**在 S8 面板里改**；写入路由只放行非密钥字段 |
| **Q36** | `server/` 按什么切 | 用户 2026-09-26 看到 `support.js` 1911 行问「不是说要解耦吗」。查下来那一份是 vendor 的构建产物（`dc-runtime` 不在我们仓库），**真正散的是 `server/`**：MCP 66 个工具（`index.ts` 1367 行）与本地 HTTP 63 条路由（`api.ts` 858 行）是**同一套核心的两个手写门面**。实测一对（`write_file` / `file_write`）：两侧都是 `writeAnyFile` 的薄包装，**唯一真实差异是 `origin: "AI"` vs `"人手改"`**，其余全是样板。用户明确目标是「加新东西要改的地方太散」，不是「文件太长」 | ✅ 定了：**能力注册表**（`server/src/cap/`）。一件能力一处声明（名字 / 入参 zod / 作用域 / 暴露给哪些门面 / `run`），MCP 面与 HTTP 面**各自遍历生成**。「谁在调」进 `CapCtx.via` —— 它是两个门面之间唯一的真实差异。`scope: "project"` 的能力**不在入参里写 `project`**：MCP 门面自动注入并解析成 ctx，HTTP 门面从服务上下文取 |
| **Q37** | 插件怎么和主程序对接 | 用户 2026-09-26 给的后期规划：不同格式的**编辑方式做成插件**，内置文本编辑，html / 视频等去插件市场买了装才有；**动态注入**，没装就不在程序里。三个理由：有的插件大、逐步加格式不用更新主程序、要能赚钱 | ✅ 定了契约形状：插件是**买来的、独立安装、独立更新**的，和主程序**必然版本错配**（用户三个月前买的插件 vs 更了五版的主程序）。所以边界**不能是「调我们的函数」**。⚠️ **2026-09-26 当天更正了一半**：原方案是「插件从 `host` 拿 `React`」，但用户随后定了「只能跑在沙盒里」（Q39），而唯一的真沙箱是**不透明源的 iframe** —— `postMessage` 只传可序列化数据，**React 对象 / 组件 / 函数一个都过不去**，`host.React` 在沙箱模型下根本不成立。改成 Figma 那套：**插件 UI 是它自己的一张网页**，住自己的 iframe，我们给一块矩形 + 一条 postMessage 桥。代价是它画不到矩形外（菜单浮层由宿主代画）；收获是**版本耦合从根上没了**，插件用什么框架我们不管。落地前提两条：① `FileKind` 从编译期联合类型改成运行期字符串表 ② 前端注册表要能**迟到注册** |
| **Q38** | 插件市场 / 授权 / 积分 | Q37 的后半段 | ✅ 用户 2026-09-26 定了四条：**离线可用** · **第一期只有我们自己上架**（三方以后再说）· **只能跑沙箱** · **一次买断，用积分买，积分可充值**；购买带**时效**概念（限时免费 / 赠送时长）。需求稿写进 `doc/20`，**P1–P6 六条还要你拍板**（其中 P1「插件能不能起外部程序」最急，它决定 M11-4 怎么写） |
| **Q39** | 沙箱用哪种 | Q38 定了「只能跑沙盒」，但沙箱有好几种，强度差很远 | ✅ 定了，**两半各关各的**（`20` §三，读数都是 2026-09-26 实测）：A 面 UI 关在 `iframe sandbox="allow-scripts"`（不给 `allow-same-origin` → 不透明源）**+ CSP `default-src 'none'`**；B 面逻辑关在独立进程走 **Node 24 权限模型**（Electron 44 内置 Node 24.21，实测可用）。⚠️ 两条实测结论：**iframe sandbox 单独用不挡网络**（`fetch` / `img` / `sendBeacon` / `WebSocket` 全能外传，加 CSP 才全挡住）；**同 VM 隔离不是边界**（Figma 的 Realms shim 被反复逃逸，已换 QuickJS-WASM）—— 所以 `vm` 模块、`Proxy` 包一层都只是防呆 |
| **Q40** | 插件能不能起外部程序 | `20` P1。视频插件八成要 ffmpeg，但**子进程不受父进程权限约束** —— 允许了等于 Node 权限模型那道墙虚设 | ✅ 用户 2026-09-26 定了：**不允许**。要外部程序由宿主提供受限能力（参数白名单），manifest 里**连 `exec` 槽位都不留** |
| **Q30** | S2 嵌入应用时只留画布，属性面板由 S11 右侧那一列装（设计侧第四轮提出，`00` §五十四） | 今天 `?embed=1` 的 S2 自带属性面板（§四十八）；S11 定了从属面板统一右侧一列，两处并存会回到「诊断在右、属性也在右但在 iframe 里」的错位 | (a) 面板搬进 S11 列：React 重画 S7 属性面板，选中 / 改值走现有 postMessage（M7-6 工作量 +2 天，但 R1 / R4 干净）· (b) 先留 S2 自带面板嵌进 S11 的面板体里过渡，M7-7 再搬。✅ **用户拍板（2026-09-24）：(b)**。M7-6 先把 S2 自带面板装进 S11 面板体过渡，M7-7 再搬进 React |
| **Q31** | 多端顺序 | 用户 2026-09-24：**先做 PC 版；Web 版与嵌进 UmbraPC 的面板版先不考虑**，PC 定了之后都好做 | ✅ 采纳：M10 里的 Web / 秘书面板后移，前端仍保持「一份前端、host adapter 隔离」的结构不变，只是不排期 |

### 4.3 M3-8 应用前端设计决策（2026-09-20）

前端无设计稿时先按以下决策落地，后面设计侧可覆盖：

| # | 决策 | 理由 | 待设计侧覆盖 |
| --- | --- | --- | --- |
| **UI-1** | 三栏布局：侧栏 280px（可调 200–400）+ 主预览区 + 底栏 220px（可收起） | 设计软件的标准布局；预览区需要最大空间 | 比例是否需要 16:9 预设；底栏是否需要可调高度 |
| **UI-2** | 稿件图标用文字缩写（"页"/"组"）而非 SVG 图标 | 简单直接，不依赖图标库 | 最终用哪种图标体系 |
| **UI-3** | 健康标签四色：绿=通过 / 黄=有提醒 / 红=错误 / 灰=未体检 | 与 `.dc.html` 入口页的契约一致 | 是否需要更多状态（渲染中 / 构建中） |
| **UI-4** | 底栏三个 tab：诊断 / 变更 / 稿件信息 | 对应 S3/S4 的界面能力 | S6 版本对比是否放入底栏或独立窗口 |
| **UI-5** | 搜索仅搜稿名、文件名、演示态名；不做全文搜索 | 便宜的搜索，M5-1 的全局搜索是另一回事 | 搜索范围是否扩大到 token / CSS 变量 |
| **UI-6** | 顶栏操作：重建索引 / 关闭项目 / 在浏览器打开 | 最常用的三个操作 | 是否需要更多快捷操作（导出 / 建稿等） |
| **UI-7** | 新建项目走两步：选目录 → 输名称/标题（prompt 对话框） | 最小流程，不需要额外 UI 文件 | 是否需要更丰富的新建向导（模板选择 / Git 开关等） |
| **UI-8** | 前端用 CSS 变量 fallback 配色，引用 `_ds-tool/tokens.css` | 与工具皮肤一致，断网可用 | 深色/浅色主题切换 |

---

## 五、已知的文档债（不是需求，是维护项）

| # | 债 | 处置 |
| --- | --- | --- |
| D1 | `00-MCP 工具契约.md` 同时装着「工具契约」和「逐批实现记录」（§十–§二十九，近 1,700 行） | M5 拆成 `00 工具契约` + `15 实现记录`（13 号已被旧文档用过，归档了也不复用编号）。现在不拆：批次记录还在高频追加，拆了会两头改 |
| D2 | `08-工具界面设计需求.md` 里 S7 的定义（组件 props 面板）和设计侧实际交付的 S7（节点属性面板）是两件事 | 已在 `08` 就地修正；组件 props 面板排进 M4 |
| D3 | `09-三层修改路径.md` 里 L2 的落点写的是「人肉转述地址」 | 已在 `09` 就地修正为「应用内会话」 |
| D4 | 设计侧交办曾散在 `10` / `13` / `14` 三份里 | ✅ 已解决：`10` 与 `13` 归档进 `_archive/`，**`14` 是唯一一份**，以后只更新它 |
| D5 | 校验器漏报：有洞却没有逻辑类 / 没引 support.js / `input[type=number][value]` | ✅ 已解决（`00` §三十、§三十一），5 条基准钉住（含 2 条边界） |
