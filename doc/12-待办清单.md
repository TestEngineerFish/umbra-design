# Umbra Studio · 12 待办清单

> 要什么看 `01-产品需求文档.md`；分期与已定决策看 `11-路线图与分期.md`。
> **这份文件只管条目、状态、验收方式、下一步** —— 不重复需求论证与决策理由。
>
> 状态：`✅ 已完成` · `🚧 在做` · `⬜ 未开始` · `🚫 明确不做`
> 每条都带**验收方式** —— 没有验收方式的条目不算需求，算想法。
>
> 编号稳定，不复用。改了状态就改这里，别另开一份清单。

---

## 〇、进度一览

| 期 | 条目 | 已完成 | 状态 |
| --- | --- | --- | --- |
| **M0** 基础能力 | 24 | **24** | ✅ 全部验收过 |
| **M1** 生命周期 | 14 | 14 | ✅ 全部验收过 |
| **M2** 应用内 AI | 13 | **13** | ✅ 三条通道都真跑通：A 直连按量、B **本地 CLI**（claude / cursor-agent 实测，`11` Q34 / Q35）、C 火山方舟订阅 |
| **M3** 应用外壳 | 8 | **7** | M3-1 到 M3-6 + M3-8 已完成，M3-7 自动更新可选 |
| **M4** 画布与对比 | 6 | **6** | ✅ 界面全部可渲染可切换 |
| **M5** 打磨 | 10 | **9** | M5-1/2/3/4/5/6/8/9/10 已完成；M5-7 明确不做 |
| **M6** 对照补齐 | 8 | **8** | ✅ 全部完成（M6-6..8 是入口对齐：首页项目列表 / 直接工作台 / 浏览器入口统一） |
| **M7** 改名 + 前端重建 | 9 | **9** | ✅ 全部完成（改名 · 新前端 · host adapter · HTTP+WS · 平移 · 布局引擎 · 旧前端退役 · 设计侧第四轮） |
| **M8** 第一批类型：目录 / `.md` / 图片 | 10 | **10** | ✅ 全部完成（`00` §五十八–§六十一）：泛型文件层 · 目录 · `.md` · 图片，三件套各自闭环 |
| **M9** 壳与分发 | 6 | **4** | M9-1 spike 过、M9-2 Electron 壳、M9-3 自带 Chromium 体检、M9-4 三平台打包（四份产物 + `packtest`）；剩 M9-6 win 真机、M9-5 公证 |
| **M10** 第二批类型 / 秘书接入 / Web 版 | 7 | 0 | ⬜（`11` Q27 / Q29） |
| 合计 | **115** | **103（90%）** · 另有 `§九` 15 条问题登记（10 修 / 3 待修 / 2 待拍板） | 2026-09-23：转向 Umbra Studio 目录工作台（`18`），新立 M7–M10 共 32 条（M9-6 是 M9-4 实测后拆出来的）；M0–M6 未完成 = M3-7（可选）+ M5-7（不做） |

⚠️ **这个百分比只数条目，不代表工作量。** 按工作量估，M2（应用内 AI）
一期大概顶 M1 + M4 两期；M0 那 24 条里也有好几条是几天的量。
真正有意义的读数是下面 §〇.1 那张能力域表。

### 〇.1 按能力域看完成度

| 能力域 | 完成度 | 缺什么 |
| --- | --- | --- |
| 稿件格式 / 运行时 | ✅ 100% | — |
| 写入口与归一化 | ✅ 100% | — |
| 静态校验 | ✅ 100% | — |
| 渲染体检 | ✅ 100% | — |
| 语义版本与四级变更 | ✅ 95% | S6 并排对比（M4-1，现为静态草图） |
| 设计系统检索 | ✅ 90% | 编辑能力（M5-2） |
| 界面 · 看 | ✅ 90% | 缩略图、多宽度预览 |
| 界面 · 编辑方式 ① | ✅ 100% | — |
| 编辑方式 ② ③ | ✅ 73% | M2 全部完成（S9 屏 + 通道 A/B + agent 循环 + 选中节点 + 改动可审） |
| **项目生命周期** | ✅ 100% | 引用图谱 + 新建 + 最近项目 + 改配 + 归档删除 + S1/M1-11 UI 入口全部完成 |
| **稿件生命周期** | ✅ 100% | 新建/改名/移动/复制/删除/恢复/文件夹分组 + S8/M1-12 UI 设置屏全部完成 |
| **画布与对比** | ✅ 100% | S6 版本对比 + S10 组件 props 面板 + 多宽度预览 + 深浅色切换 + 缩略图 + 索引过期横幅全部完成 |
| **应用内 AI** | ✅ 73% | M2 全部完成：S9 会话面板 + 通道 A agent 循环（21 工具） + 通道 B Claude Code + 选中节点上下文 + 改动可审 + 用量显示 + agenttest |
| 应用外壳 / 分发 | ✅ 88% | M3-1~6/8 完成；M3-7 自动更新可选 |

### 〇.2 下一步做什么

2026-09-24：M7-1 改名、M9-1 spike（结论换 Electron）、M7-9 第四轮发件都已完成。**顺序按 spike 结论调整**：
新前端直接在最终的壳里平移，不在要退役的 Tauri 上做两遍。

1. **M7-2 新前端骨架**（`app/`，Vite + React + TS + Tailwind）+ **M7-3 host adapter**（先只做 `browser` 实现）+ **M7-4 核心侧 HTTP / WS**
2. **M9-2 Electron 壳**：主进程起核心、`desktop` adapter、菜单、单实例；Tauri 壳与 sidecar 目录退役；**M9-3 随之成立**（体检走壳自带 Chromium，`00` §五十 的 `UMBRASTUDIO_CDP` 通道）
3. **M7-5 / M7-6 平移**现有能力到新前端（在 Electron 壳 + 浏览器两边都跑；不加新功能；`selftest` + `rendertest` + 壳内自测全过）
4. **M7-7 布局引擎 R1–R5** → **M7-8 旧前端退役**
5. **M7-9 设计侧第四轮**：已发，等它交回 S11 + 第 1 题答复，按 `incoming` 并入
6. **M8** 按「目录 → `.md` → 图片」加类型，每种三件套一起交；M10 最后；**M2-3 通道 B 已于 2026-09-24 用本机 Claude Code 登录态打通**（`11` Q34）
7. 纪律：前端只有 `app/` 一份（旧 vanilla 前端已于 M7-8 删除，`00` §五十七）
8. **先做 PC 版**（`11` Q31，2026-09-24 用户定）：Web 版、嵌入 UmbraPC 的面板版不排期；Q30 按 (b) 分两步

---

## 一、M0 · 已完成（基线）

这一节是**已经验收过的能力**，列出来是为了让「剩多少」有分母。
每条后面是它的出处章节。

| # | 事情 | 出处 |
| --- | --- | --- |
| M0-1 | MCP server 骨架（stdio，统一信封 `{ok,data,errors,warnings,stats}`） | `00` §二、§四 |
| M0-2 | 租户与 `@ds` 展开 | `00` §三 |
| M0-3 | 检索类工具（token / 图标 / 组件 / 写稿规则，共 9 个） | `00` §5.1 |
| M0-4 | 唯一写入口 `write_draft` / `patch_draft`（归一化 · `@ds` · `__resources` 注入） | `00` §5.2、§十五 |
| M0-5 | `validate_draft` 静态校验，诊断带 code / 行号 / 洞名 / 改法 | `00` §六 |
| M0-6 | `render_check` 真浏览器体检，判活只认 `1+1` | `00` §七 |
| M0-7 | 语义快照 + 四级 diff + `CHANGELOG-设计侧.md` | `07` |
| M0-8 | 本地静态服务（形态 A）+ `build_index` 入口页 | `00` §十四 |
| M0-9 | 本地 JSON API `/__ud/*`（随机令牌 + Origin 校验） | `00` §二十 |
| M0-10 | 作业注册表（长任务 POST 起 / GET 轮询） | `00` §二十三 |
| M0-11 | 节点地址 `data-ud-node`（内容哈希 + 去重序号） | `09` §二 |
| M0-12 | 逐洞来源审计（字面量 / props / 计算 / 函数 / 认不出） | `00` §十六、`09` §3.1 |
| M0-13 | 属性级写入 `set_prop`（只改一处，是洞就拒绝并说改哪个键） | `09` §3.2 |
| M0-14 | 版本回退 `revert_to`（**向前**的操作，历史只增不改） | `00` §18.3 |
| M0-15 | 样式覆盖通道（拖动中不落盘） | `00` §二十五 |
| M0-16 | 点选桥（注入预览，不进稿） | `00` §二十二 |
| M0-17 | S1 稿件索引（入口页） | `08` S1 |
| M0-18 | S2 单稿预览壳 + **节点属性面板（编辑方式 ①）** | `08` S2、`00` §二十七 |
| M0-19 | S3 诊断面板 | `08` S3 |
| M0-20 | S4 变更清单（四级 + 项目级汇总） | `08` S4、`00` §二十六 |
| M0-21 | S5 设计系统浏览器 | `08` S5 |
| M0-22 | 回归三层：`fixtures/静态` 14 份 · `ui/` 7 份 · 语料（可选）；`fixtures/渲染` 15 份 | `00` §二十八 |
| M0-23 | 交付接入 `ui/_incoming/` + `npm run incoming` | `00` §二十七 |
| M0-24 | 人用入口 `npm run ui`；`build_index` 自维护租户 `.gitignore` | `00` §29.3、§29.4 |

---

## 二、M1 · 项目与稿件生命周期

**这一期的目标**：从一个空目录开始，全程只用界面，走完「建项目 → 建稿 →
改名 → 移动 → 复制 → 删除 → 恢复」，期间 `dc-import` 引用一次都不断。

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M1-0 | **引用图谱**：`list_references(file)` | 回答「谁 import 了我」「我 import 了谁」。全部生命周期操作的前置（`11` §3.3） | 语料里 `Umbra PC 端`（115 个 import）能正确列出双向引用；孤立稿返回空 |
| ✅ M1-1 | `create_project` | 建目录 + `project.json` + `.gitignore` + 可选设计系统骨架 + 第一份稿 | 空目录起步建成，立刻 `get_project` 能读、`npm run ui` 能开 |
| ✅ M1-2 | 工作区与最近项目 | 项目可在任意路径；`projects/` 降级为默认新建位置（依赖 Q5）。**2026-09-23 补**：`loadProject` 与壳的 `open_project_command` 原来只认 projects/ 根下的名字，任意路径在壳里打不开（`00` §三十八），已修 | 在三个不同路径各建一个项目，最近列表按时间排；删掉目录后列表里标「找不到」而不是崩 |
| ✅ M1-3 | `create_draft` | 四种来源：空白骨架 / 模板 / 复制现有稿 / 把组件包一层 | 四种各建一份，全部 `validate_draft` 零 error、`render_check` 活着 |
| ✅ M1-4 | `rename_draft` | **连带改所有引用它的 `dc-import name`** | 改一个被 3 份稿引用的组件名，改完三份都仍解析得开（`E_IMPORT_MISSING` 为 0） |
| ✅ M1-5 | `duplicate_draft` | 复制含快照要不要跟着复制（我的建议：不跟，新稿从 v1 起） | 复制后两份互不影响，改一份另一份不变 |
| ✅ M1-6 | `move_draft` | 跨目录移动，引用路径要跟着修（相对路径基准变了） | 把一份被跨目录引用的组件移进子目录，引用仍解析得开 |
| ✅ M1-7 | `delete_draft` | 回收站语义（依赖 Q4）；删前列出受影响的引用方并要求确认 | 删一个被引用的组件要先告警；删完能从回收站恢复，恢复后引用恢复 |
| ✅ M1-8 | 文件夹分组 | 建 / 改名 / 移动目录（依赖 Q9） | 建两级目录并把稿分进去，S1 能按目录分组显示 |
| ✅ M1-9 | `update_project` / 项目设置 | 改设计系统路径、限额、标题 | 改完 `get_project` 读到新值，元素数限额立刻生效于下一次校验 |
| ✅ M1-10 | `delete_project` / 归档 | 归档 = 打包成一个目录挪走；删除要二次确认 | 归档后项目消失于列表、目录仍在；删除后目录进回收站 |
| ✅ M1-11 | S1 上的生命周期入口 | 新建 / 改名 / 复制 / 移动 / 删除 / 回收站 / 项目切换器。**2026-09-23**：设计侧第三轮交回行内撤销形制，`delete_draft` / `restore_draft` 路由已接（`00` §四十五） | 【实测】删除 → 塌行 → 撤销 → 文件回来 |
| ✅ M1-12 | 新屏 **S8 项目设置** | 设计侧形制已收（第二轮）；**2026-09-23 接真数据并嵌进应用设置面板**（`00` §三十九）：基本信息 / 设计系统 / 限额改完即存、回收站恢复 / 彻底删除 / 清空、归档 / 删除行内确认 | 【实测】改标题与限额落盘；删稿 → 回收站列出 → 恢复 → 列表 1 → 2 |
| ✅ M1-13 | 生命周期回归进 `fixtures/` | 脚本化走一遍全流程，每步后校验 | `npm run lifecycletest` 全绿；故意打断引用能被抓到 |

---

## 三、M2 · 应用内 AI（编辑方式 ②③）

**这一期的目标**：三种编辑方式都在同一个窗口里完成。
大脑走两条通道 —— 依据与合规限制见 `01` §六，架构见 `11` §2.2。

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M2-1 | **通道 A · provider 适配器** | 2026-09-23 **两家都真跑通**（`00` §35.1、§35.4）：修掉端点拼接丢路径、messages 少用户那句；agent 加 `read_draft`（DeepSeek 没它找不到源码）。一句「改成 danger 态」→ 模型调工具写 v2、L2 ×1、回退成功 | 智谱 ✅ 23.6 s · DeepSeek ✅ 3.3 s |
| ✅ M2-2 | 密钥与端点配置 | 存本地，**不进任何日志、不进 changelog、不随项目走**（密钥属于机器，不属于项目） | 配置改完立刻生效；密钥不出现在任何落盘文件里（grep 验证） |
| ✅ M2-3 | **通道 B · 子进程 Claude Code** | 2026-09-24 打通（`00` §六十四，`11` Q34）：改成**用本机已登录的 Claude Code**（`baseUrl`/`apiKey` 留空即可，不必等 GLM 套餐）。顺带修四条：硬编码的 23 个工具名换成 server 级放行（实际已有 66 件）、系统提示补当前项目绝对路径（原来它只能 `list_projects` 猜，120 s 超时零工具调用）、`--output-format` 换 `stream-json`（`json` 模式拿不到工具调用事件）、去掉 `--brief`（它让模型改用 `SendUserMessage`，`result` 就空了）。加 `--strict-mcp-config`（原来继承用户 54 台 MCP server）、`--setting-sources ""`、`--max-budget-usd` | 【实测】真跑改 `.md`：14.9 s · 5 轮 · `ToolSearch → list_files → read_file → write_file` · 两处都改对 · 快照 s2 · 回答拿到 |
| ✅ M2-13 | **本地 CLI 通道**（`11` Q35） | 2026-09-24（`00` §六十五）：`local_cli.ts` 抽出 `CLI_SPECS` / `detectLocalClis` / `runLocalCli` / 各家 parse，`channel_b.ts` 删掉只留一处实现。五家：**claude / cursor-agent / codex 三家实测跑通**（codex 按文档写的三个字段实测全错，`00` §65.6），gemini / opencode 按文档写、标 `verified:false`。S8 面板里能选 CLI 与模型，能扫环境、能问可用模型（cursor-agent 列出 31 个）。写入路由只放行非密钥字段 | 【实测】claude 15.9 s · cursor-agent 登录态 29.1 s · codex 33.4 s，三家都 `read_file → write_file` 改对；界面真开浏览器看过：五家都列出、能力标签与实测一致、Cursor 的「列一下」拉到 31 个模型 |
| ✅ M2-4 | 两条通道统一到同一接口 | 界面上只是「用哪个大脑」，切换不改会话形状、不改可审口径 | 同一句提问在两条通道下都能完成，改动可审性一致 |
| ✅ M2-5 | 会话模型 | 消息 / 工具调用 / **步级流式 / 真中断**（2026-09-23 作业化，`00` §四十）；与项目绑定，存 `.umbrastudio/chats/`。token 级逐字流式未做 | 【实测】跑到一半能看到工具行长出来；中断 0.94 s 停下并记「已中断」；关掉重开会话还在 |
| ✅ M2-6 | agent 循环（仅通道 A 需要） | 21 个工具已接入 `executeToolCall`：get_project、list_drafts、search_tokens、get_token、list_components、get_component、list_icons、get_icon、get_syntax_guide、validate_draft、write_draft、patch_draft、render_check、list_versions、diff_drafts、snapshot_draft、get_changes_since、revert_to、locate_node、set_prop、list_references。模型能连续调 5 个以上工具 | 一次请求里连续调 5 个以上工具能跑完；中途出错有结构化回报 |
| ✅ M2-7 | 新屏 **S9 会话面板** | 2026-09-21 修好：补骨架 + 演示条；render_check alive ✓ / 144 节点 / 未解析洞 0 / 演示态可切 | 一边看稿一边对话；面板可收起 |
| ✅ M2-8 | **方式 ②**：选中节点带进会话 | chat_send 带 selectedNodeFile + selectedNodeAddress → locateNode 解析成系统提示。**2026-09-23 在应用里闭环**（`00` §37.1）：前端预览走 S2 壳（`embed=1`），选中节点 postMessage 上来，会话带地址发送 | 【实测】选中按钮说「这里字号大一点」→ 只有它多了 `font-size 20px`，L2 ×1 |
| ✅ M2-9 | 改动可审 | `chat_send` 响应中自动包含 `changes` 字段：记录 agent 循环前后各稿版本对比，四级分级（L1-L4）+ 人类可读摘要。通道 A/B 都有 | `01` §7.6 第 23 条 |
| ✅ M2-10 | 用量显示 | 通道 A 返回 `usage`（promptTokens / completionTokens / totalTokens）；通道 B 返回 `usage`（inputTokens / outputTokens / totalCostUSD）+ `toolCalls`（模型调了哪些工具）+ `numTurns` | 一次对话后能看到读数 |
| ✅ M2-12 | **S9 会话面板接进 Tauri 前端**（2026-09-23 立项当天落地，`00` §三十六） | 右侧栏按 S9 形制；`chat_run.ts` 让 MCP 与本地 API 共用一份；`originOk` 放行 Tauri 来源（顺带修掉壳里 POST 全 403）。方式 ② 待 S2 壳与前端打通 | 【实测】前端发一句 → 工具行 + 变更卡 L2 ×1（v6 → v7）→ 一键回退 v8 |
| ✅ M2-11 | `npm run agenttest` | 固定提问 + 固定稿，**不判措辞**，只判三件：改动落点（L2 取值变更 1 处） / 四级分级（counts 字段正确） / 能否回退（颜色恢复） | 全绿；换一次模型仍然全绿（判据不依赖具体模型，直接调业务函数模拟 agent） |

## 四、M3 · 应用外壳与分发

| # | 事情 | 验收 |
| --- | --- | --- |
| ✅ M3-1 | 选型落地（Tauri 2） | `src-tauri/` 下是 Rust 项目，`tauri build` 产出 `.app` + `.dmg`（arm64） |
| ✅ M3-2 | 窗口 / 菜单 / 单实例 / 最近项目 | 单实例聚焦、原生菜单栏、最近项目列表持久化 |
| ✅ M3-3 | server 作为 sidecar 启停 + 端口令牌交接 | 启动自动 spawn 子进程、退出 SIGTERM 清理、三个 Tauri 命令 |
| ✅ M3-4 | 文件夹选择对话框 | 新建 / 打开项目走系统对话框；通过 sidecar 调用 create_project |
| ✅ M3-5 | 打包 `.app` / `.exe`（自带 Node） | 106MB Node 捆绑进 .app；DMG 38MB；干净机器双击能用 |
| ✅ M3-6 | 崩溃恢复与日志 | Session marker 检测非正常退出；on_window_event 清理 sidecar；tauri-plugin-log |
| ✅ M3-8 | 应用前端（项目 → 稿件 → 预览） | `server/ui/index.html`：Welcome / 项目 / 稿件列表 / S2 壳预览 / 底栏 / 会话栏。**2026-09-23 壳内自测**（`00` §三十八）抓到并修掉三条只在壳里出现的缺陷：`invoke_mcp` 参数名、任意路径打不开、dev 来源 403 |
| ⬜ M3-7 | 自动更新（可选） | 能检测到新版本；失败不影响使用 |

---

## 五、M4 · 画布与对比

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M4-1 | 新屏 **S6 版本对比** | 2026-09-21 修好，实测同上（149 节点）。| 两版并排，滚动同步，变更处高亮。和 S4 是同一份数据的另一种投影 | 对一份改过 3 次的稿，任选两版能并排看出差异，高亮位置和 S4 的行号对得上 |
| ✅ M4-2 | **组件稿 props 面板** | 2026-09-21 修好；顺带修掉 `input[type=number][value]` 的解析期告警（`00` §三十一）。| ⚠️ `08` S7 原本定义的是这个（把 `data-props` 里 `editor` 非 null 的项渲染成控件），设计侧交的 S7 是**节点属性面板**，两件事 | 打开一个组件稿，改 props 实时重渲染（走运行时的 `__dcSetProps`），演示态切换钮不混进这个面板 |
| ✅ M4-3 | 多宽度 / 设备预设预览 | S2 已有 zoom 与 preset 的状态位，没接真数据 | 一份稿在 3 个宽度下切换，不重载 iframe |
| ✅ M4-4 | 稿的深浅色切换 | 改的是稿自己的 theme，不是工具皮肤 | 带 `color.dark` 的稿能切换并看出差别 |
| ✅ M4-5 | 缩略图生成 | S1 卡片形态要图；`render_check` 已经能截图 | 建索引时生成，稿改过之后能看出图过期 |
| ✅ M4-6 | S1 索引过期横幅形制 | `doc/10` §五 第 11 项，已在 S1 实现 | 设计侧给形制后替换我定的那一版 |

---

## 六、M5 · 打磨

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M5-1 | 全局搜索 | 跨稿找 token 用法 / 文案 / 组件引用；四种匹配类型（token/import/css/text） | 搜一个 token 名，列出所有用到它的稿与行号 |
| ✅ M5-2 | 设计系统编辑（依赖 Q7） | set_token_value 只改取值不改结构；改前自动分析影响面 | 改一个 token，事先列出受影响的稿数；改完那些稿的渲染确实变了 |
| ✅ M5-3 | 并发写保护（依赖 Q8） | writeDraft/patchDraft 支持 expectedSourceSha256；不一致拒绝 | 两个客户端同时改同一份稿，后写的那个被拒绝并说清原因 |
| ✅ M5-4 | 稿件模板库 | list_templates / save_as_template / delete_template；create_draft 支持 templateName | 存一份稿为模板，新建时能选到 |
| ✅ M5-5 | 项目导出 / 迁移 | export_project / import_project 用系统 tar；含快照、changelog | 导出再导入到另一台机器，版本历史完整 |
| ✅ M5-6 | 键盘与可达性过一遍 | 2026-09-23 第一遍（`00` §37.2）：列表 ↑↓ / Enter / Home / End、`/` 搜索、`Cmd/Ctrl+J` 会话、Esc 关面板、focus-visible。面板内焦点圈死与底栏 tab 方向键留作打磨 | 主流程键盘能走：/ 搜 → ↓ 选稿 → Enter 开 → ⌘J 说话 → Enter 发 |
| 🚫 M5-7 | `bundle_draft`（形态 B） | 两点仍未验证，从第一批起就没承诺（`00` §九） | — |
| ✅ M5-8 | S1 索引过期（设计侧裁决 3.1） | 2026-09-23：设计侧在新底稿上交回（incoming 底稿正确、接线 4/4）；新增 `POST rebuild_index`，S1 点「重建索引」→ 重建 + 整页重载（`00` §33.1） | 【实测】touch 一份稿 → 横条 + 行级「文件已改」+ 三列灰 → 点重建 → 横条消失、版本列刷新 |
| ✅ M5-9 | S2 版本弹层 + 未落盘横条（裁决 3.2 / 3.3①） | 2026-09-23：版本元数据 `snapshots/<稿>/meta.json`（来源 新建 / AI / 人手改 · 时间 · 最重一条变更），`changes` 路由带 `versionMeta`；横条「落盘」接 `applyProp`；顺带修掉两次并发写撞临时文件（`00` §33.2） | 【实测】弹层列 v1–v4 带来源与摘要；未落盘药丸 `[data-ud-node="fec4d9c9"] · style.padding → 4px 8px`，点落盘 v5 → v6 |
| ✅ M5-10 | 应用前端 UI-1..UI-8 | 2026-09-23 落地（`00` §三十四）：底栏两档 + 把手；SVG 稿件图标；渲染中态；「对比上一版」→ S6 独立窗口（S6 已接真数据）；顶栏「新建稿件」+ ⋯ 菜单；新建项目面板 + `inspect_dir`；主题三档。顺带修掉体检轮询永远转不完 | 【实测】浏览器调试模式逐条走过：半屏 / 收起、图标、渲染中 → 通过、S6 新窗口两栏真版本、菜单、新建稿件 / 新建项目面板、浅色主题 |

---

## 六之二、M6 · 对照 ClaudeDesign 补齐（2026-09-23 立项，依据 `16`，决策 `11` Q13–Q15）

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M6-1 | 会话栏默认在左 + 开关 | 2026-09-23（`00` §四十二）：布局开关在设置面板，默认在左；稿件列表可收成 36px 窄条 | 【实测】列序随开关翻转、localStorage 记住；收起 36px 可展开 |
| ✅ M6-2 | 钉在节点上的评论 | 2026-09-23（`00` §四十四）：`comments.json` + 四条路由 + MCP `list_comments`；S2 面板评论区、桥画钉子；应用底栏「评论」tab，「发给 AI」走方式 ② | 【实测】加评论 → 钉子 1 → 发给 AI 只改 `font-size 24→28`（v17）→ 已处理 → 钉子 0 |
| ✅ M6-3 | Code 只读视图 | 2026-09-23：预览第四档「源码」，`source` 路由，行号 + 选中节点行高亮，改稿后随 reloadPreview 刷新 | 【实测】19 行带行号 |
| ✅ M6-4 | Present 全屏 | 2026-09-23：应用预览工具栏「演示」→ 全屏覆盖层只装稿本身，Esc 退出（S2 里的入口随 M6-5 一起加） | 【实测】覆盖层装稿本身，Esc 收掉 |
| ✅ M6-6 | 首页 = 项目列表 | 2026-09-23（`00` §四十六）：列表 / 网格、搜索、星标、最近打开、缩略图（有体检截图的稿）；`projects` / `open_project` 路由 | 【实测】真项目三个列出，切换项目跳到它的服务 |
| ✅ M6-7 | 进项目直接工作台 + 文件页签 | 自动开上次看的稿；稿件列表默认收起成两栏；预览顶栏文件页签下拉（搜索 / 新建 / 展开列表） | 【实测】`/__app/` 打开即左会话右预览，页签 29 行 |
| ✅ M6-8 | 浏览器入口统一到应用本体 | sidecar 托管 `/__app/`（同源、令牌注入），`npm run ui` 打开它；S1 退成备用入口页 | 【实测】浏览器里和 Tauri 壳同一份界面 |
| ✅ M6-5 | 文字就地编辑 | 2026-09-23（`00` §四十三）：点选模式下双击 → locate 判定 → contenteditable → Enter / 失焦走 `applyProp` 落盘，Esc 还原；洞给来源说明。顺带把应用前端改成分区渲染，预览 iframe 不再随重绘重载 | 【实测】双击改「提交 → 保存草稿」回车 → v15，L3 ×1 |

## 六之三、M7 · 改名 Umbra Studio + 前端重建（2026-09-23 立项，依据 `18` §九，决策 `11` Q18 / Q21 / Q25 / Q28）

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M7-1 | 改名 | 2026-09-24（`00` §四十九）。GitHub 仓库 `umbra-design` → `umbra-studio`（**用户在 GitHub 上操作**，自动重定向）、本地目录 `UmbraStudio`、`server` 包名、MCP server 名 `umbrastudio`、项目配置目录 `.umbradesign/` → `.umbrastudio/`（打开旧项目时自动拷一次，旧目录不删）、`01` / `README` / `CLAUDE.md` / 各 doc 标题全文换名；`.dc.html` 格式与 `_ds` 约定不改 | 【实测】`grep -ri umbrastudio` 只剩 git 历史、`_archive/` 与 `18`；旧项目打开一次后 `.umbrastudio/` 存在且快照序列连续 |
| ✅ M7-2 | 新前端工程骨架 | 2026-09-24（`00` §五十二）。`app/`：Vite + React + TS + Tailwind，与 UmbraPC 同栈；构建产物由核心托管在 `/__app/`；旧 `server/ui/index.html` 挂 `/__legacy/` 保留到 M7-8 | 【实测】断网 `npm run build` 产物可开；`/__app/` 打开是新前端 |
| ✅ M7-3 | host adapter | 2026-09-24：接口 + `browser`（`00` §五十二）+ `desktop`（Electron preload → `window.umbraHost`，`00` §五十三）。接口：`pickDirectory` / `revealInFinder` / `openExternal` / `notify` / `setTitle` + `onEvent`；不可用的说明原因 | 【实测】`01` 第 35 条前半：`grep -r "electron\|tauri" app/src` 只命中 `host/` 目录 |
| ✅ M7-4 | 前端 ⇄ 核心通道（核心侧） | 2026-09-24（`00` §五十二）：`events.ts` 总线 + `/__ud/ws`（job / chat / write / fs）+ 目录监听。HTTP + WS（会话流式、文件变更推送）在核心侧做好，新前端只走这一条；**Tauri 壳里那条 stdio 直连不再动，随 M9-2 一起退役**（2026-09-24 调整：Q26 已定换 Electron，不在要退役的壳上做通道合一） | 【实测】新前端所有操作走 HTTP / WS；`lib.rs` 未改 |
| ✅ M7-5 | 平移：首页 / 项目列表 | 2026-09-24（`00` §五十五）：列表 / 网格、搜索、星标、最近打开、缩略图、导入目录（host.pickDirectory；浏览器降级为粘路径）、新建项目面板、在访达中显示 | 【实测】Playwright：6 行 / 6 卡 / 搜索 5；壳里菜单「打开目录」→ 事件 → 打开 |
| ✅ M7-6 | 平移：`.dc.html` 工作台 | 2026-09-24（`00` §五十五）：S11 形制（顶栏 / 页签 / 会话栏三态 / 画布一条工具栏 / 右侧图标轨 + 面板体 / 状态行）；S2 嵌入壳（Q30 过渡：属性面板铺进面板体）、诊断、变更 + 版本历史回退、评论 + 发给 AI、源码、演示、体检、新建稿件、设置嵌 S8、会话作业化 | 【实测】落盘 → WS → 变更面板 → 回退闭环；方式 ② 链路到 provider 调用为止（DeepSeek 402 余额不足，AI 本身未跑）；壳测试全过 |
| ✅ M7-7 | 布局引擎 R1–R5 | 2026-09-24（`00` §五十七）：类型预设、会话栏三态 + 可拖宽、从属面板按类型出现、每类收放记忆（`panelByKind`）、< 1100 px 抽屉；**属性面板搬进 React**（Q30 第二步做完，S2 嵌入只剩画布）；「已选中」药丸泛化五种可多颗 | 【实测】`01` 第 26–28 条；改值预览不落盘 → 回车落盘 → 绿条 → 撤销全链路 |
| ✅ M7-8 | 旧前端退役 | 2026-09-24：删 `server/ui/`（1729 行）与 `/__legacy/` 路由；`npm run ui` 只开 `/__app/` | 【实测】仓库里没有 vanilla 前端；壳测试在新前端跑通 |
| ✅ M7-9 | 设计侧第四轮 | 2026-09-24 全部收齐（`00` §五十四、§五十六）：S11 + 第 1 题；S12–S15 + S1 / S9 改动，六份零 error 并入，质感四条逐屏过。它问的四件已定（行号计入 frontmatter；`snapshots[]`、`referencedBy[{file,line?}]`、`viewByDir` 采纳）。接线随 M8。`doc/14` 第四轮：S11–S15 新屏 + S1 / S9 改动（`08` §三之三）；**设计与开发并行**，形制回来后按 `incoming` 并入，前端按稿调整 | 【实测】`incoming` 零 blocking；S11–S15 演示态可切；前端形制与稿一致 |

## 六之四、M8 · 第一批类型：目录 / `.md` / 图片（依据 `01` §4.2，决策 `11` Q19 / Q22 / Q23 / Q24）

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M8-1 | 泛型写入口 | 2026-09-24（`00` §五十八）：`files.ts`，写前 sha256 校验 → 旧版快照 → 原子写 → 新版快照 `s<N>`；`.dc.html` 被拒并指向 `write_draft`；回退 / 回收站 / 引用查询一并有 | 【实测】`filetest` 19 条全过（含 frontmatter 逐字节不变、过期 sha 被拒） |
| ✅ M8-2 | 泛型 MCP 工具 | 2026-09-24：MCP `list_files` / `read_file` / `write_file` / `move_file` / `list_file_versions`（共 65 个工具）+ 九条 HTTP 路由；返回含 kind、大小、尺寸、快照号 | 【实测】`filetest` 走的就是同一层；MCP 侧随 M8-3 前端接入一并复跑 |
| ✅ M8-3 | 目录视图 | 2026-09-24（`00` §五十九）：面包屑 / 列表 / 网格（图片多时自动切并说明）/ 只看稿件 / 读数按类型换 / 常驻勾选框 / 多选条（移动 · 删除 · 带进会话）；改名移动删除都泛化到非稿文件 | 【实测】`01` 第 25 条通过：纯 md + 图片 + json 的目录直接进目录视图 |
| ✅ M8-4 | 目录 → 会话上下文 | 2026-09-24：`files` 药丸 + `chat_send.selectedFiles`；服务端把路径 / 类型 / 大小 / 头三行拼进系统提示（通道 A / B 都接） | 【实测】`01` 第 32 条的机制部分通过（请求里带了三条路径）；AI 回合等 DeepSeek 余额 |
| ✅ M8-5 | 通用文件卡 | 2026-09-24：S15 形制，元数据 + 被引用（`file:line` 可点）+ 改名 / 移动 / 访达 / 删除（行内二次确认）；禁用项原因写在按钮下 | 【实测】zip 出卡片、引用列出 `PC 吐司.dc.html:12` |
| ✅ M8-6 | `.md` 预览与大纲 | 2026-09-24（`00` §六十）：markdown-it 打进 `app/` 构建产物（不是 `runtime/`，理由见 §六十）；渲染 / 源码切换；frontmatter 收成一行可展开；大纲在右侧那一列，行号计入 frontmatter | 【实测】断网可渲染；大纲三条、点条目跳到源码第 12 行 |
| ✅ M8-7 | `.md` 源码编辑 | 2026-09-24：带行号的文本框（**不引 CodeMirror**，理由见 `00` §六十）；失焦 / ⌘S 落盘走 `file_write`；未落盘齐边横条 + 放弃 / 落盘；快照弹层可回退 | 【实测】`01` 第 29 条通过：改一行 → s2 → 回退 → 内容回来、frontmatter 逐字节不变 |
| ✅ M8-8 | `.md` 选中段落 → AI | 2026-09-24：`range` 药丸 + `chat_send.selectedRange`；**泛型文件四件工具补进 agent 表**（之前 AI 拿不到，只会撞 patch_draft 的扩展名检查） | 【实测】`01` 第 30 条通过：只有 `## 范围` 变了，头部与 `## 状态` 逐字节原样 |
| ✅ M8-9 | 图片视图 | 2026-09-24（`00` §六十一）：S14 形制 —— 棋盘格底、缩放 25–400 %、`0` 适配、SVG 矢量标签 + 默认 200 %；尺寸从文件头读 | 【实测】PNG / SVG 实开；`filetest` 钉住 PNG / GIF / WebP / JPEG / SVG 的尺寸解析 |
| ✅ M8-10 | 图片圈选 → AI | 2026-09-24：拖框（原图像素）+ 备注 → `region` 药丸 → 多模态消息（裁图 + 坐标 + 我们自己算的方位）；`supportsImage` 由**探一次**定（`probe_image_support`），不是靠模型名猜；不支持时入口禁用且原因常显、不发图 | 【实测】`01` 第 31 条通过：AI 引用坐标与方位并答对颜色（`00` §六十一） |

## 六之五、M9 · 壳与分发（决策 `11` Q26 待 spike）

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M9-1 | 壳 spike（半天） | 2026-09-24 两条都过（`00` §五十）：① `render.ts` 加 `UMBRASTUDIO_CDP` 通道，Electron 自带 Chromium 的读数与系统 Chrome 逐项一致、墙钟更短；② 核心 `import` 进主进程，MCP 客户端 initialize / 60 工具 / render_check 全程走通 | 【实测】读数在 `00` §五十；Q26 已回填 → 做 M9-2 |
| ✅ M9-2 | Electron 壳 | 2026-09-24（`00` §五十三）：`shell/main.mjs` 主进程 import 核心、起 hub 服务；`--mcp` 时 stdio 归 MCP；preload 挂 `window.umbraHost`；菜单（打开目录 / 最近打开 / 回项目列表）；单实例；`session.json` 判上次没正常退出 → 前端提示。**Tauri 与 sidecar 已删**。 | 【实测】`shell/shelltest.mjs`（Playwright `_electron`）全过；`01` 第 36 条的「干净机器」未验，打包产物在本机可开 |
| ✅ M9-3 | 体检不依赖系统 Chrome | 2026-09-24：壳开隐藏窗口 `about:blank#umbrastudio-check`，`UMBRASTUDIO_CDP` 指向自带 Chromium；`render.ts` 按标记找页、CDP 模式串行 | 【实测】壳里 `check` 作业 alive · 24 节点 · 1430 ms，期间系统 Chrome 进程数 0（`00` §五十三）；「没装 Chrome 的机器」未验 |
| ✅ M9-4 | 打包三平台 | 2026-09-24：mac arm64/x64 出 dmg + zip、win x64/arm64 出 zip；`afterPack` 补完整 ad-hoc 签名；`STATE_ROOT` 把可写状态搬到 userData；`doc/` 进包。顺带修三条打包版才会炸的缺陷（`00` §六十三） | 【实测】`packtest` mac arm64 34/34 · mac x64 34/34（Rosetta）· win x64 / arm64 各 17/17（结构关）。拷到仓库外的目录 + 全新 userData 跑通，体检 `browser.from=UMBRASTUDIO_CDP` |
| ⬜ M9-6 | win 真机第一次跑 | 把 `Umbra Studio-0.1.0-win-x64.zip` 解到一台 Windows 上，走 `01` 第 35 条的流程；`packtest` 的二、三、四关在 win 上重跑一遍 | 【实测】待 |
| ⬜ M9-5 | 公证与自动更新 | 公证：买 Apple 开发者证书 + `notarytool`，免掉用户「右键打开」那一步（现在 `spctl` 拦，理由是未公证，不是缺陷，`00` §63.3）。自动更新可选，未排 | — |

## 六之六、M10 · 第二批类型 / 秘书接入 / Web 版（决策 `11` Q27 / Q29）

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ⬜ M10-1 | 秘书接入（MCP） | UmbraPC 作为 MCP 客户端连本机核心（stdio） | 【实测】`01` 第 34 条 |
| ⬜ M10-2 | 第二批类型：代码 / 文本 | CodeMirror 只读高亮 + 改字面量 + 选中行 → AI | 【实测】选中行进会话；改一处快照 +1 |
| ⬜ M10-3 | 第二批类型：非 dc html | iframe（本地 http）+ 点选元素带 `outerHTML` | 【实测】非 dc 页面点选一次进会话 |
| ⬜ M10-4 | 核心远端部署 | ECS 独立进程、每账号工作区、MCP 走 HTTP 传输 | 【实测】浏览器打开远端前端走完「打开 → 改 → 回退」 |
| ⬜ M10-5 | 账号接入 | 依赖 Umbra 多账号 | 【实测】两个账号的工作区互不可见 |
| ⬜ M10-6 | 体检沙箱 | 容器 / 独立用户 + 超时 + 禁网；**Web 版上线前置** | 【实测】一份带外网请求与死循环的稿：请求被拒、超时被杀 |
| ⬜ M10-7 | Studio 面板嵌进 UmbraPC | 同栈嵌入 | 【实测】UmbraPC 里打开目录看 `.md` |

## 七、被取代 / 已作废的条目

| 原条目 | 处置 |
| --- | --- |
| 「L2 的最后一环：选中后一颗『复制给模型』按钮」 | **被 M2-6 取代**。有了应用内会话就不需要人肉搬地址了 |
| `doc/10` §三：`decideMeta` / `decideActions` 的内容写进 `任务.dc.html` | **不是我们的事**。那是测试项目里的稿，归项目自己（和用它的 AI）。回归里那 4 条 error 是工具在正常报活 |
| `doc/01` §二「第一阶段调用方是模型，不是人」 | **已过期**，见 `11` §1.2。人是第一使用者 |
| `doc/09` 三层修改路径的 L2/L3 定义 | 仍有效，但 L2 的落点从「人肉转述」变成 M2 的会话 |

---

## 八、决策已全部拍板

Q1–Q29 的结论记在 `11` §四，不在这里重复。摘两条影响条目形状的：

- **Q2**：DeepSeek 直连 + 智谱 GLM 订阅。因订阅限官方工具，落成两条通道 —— 这是 M2 由 9 条变 11 条的原因。
- **Q10**：需求文档整篇重写，冲突内容删除不保留；需求相关三份文件职责互斥（`11` §4.2）。

新的不确定项出现时，登记到 `11` §四下面，不要在本文件里开新的问答区。

## 九、问题登记（GitHub issues · 2026-09-23 起）

规范在 `17`；草稿与提交脚本在仓库根 `issues/`。issue 号提交后回填。
**当前 token 建不了 issue / 标签（细粒度 PAT 缺 Issues 权限，403）**，草稿已就绪，权限补上后跑 `issues/post.sh issues/2026-09-23`。

| # | 草稿 | 级 | 状态 | issue |
| --- | --- | --- | --- | --- |
| 1 | [bug] 首页打开从未建索引的项目失败（用户报） | p0 | ✅ 已修 `b41379b` | — |
| 2 | [bug] 浏览器模式「新建稿件」「新建项目」不可用 | p1 | ✅ 已修 | — |
| 3 | [chore] 「打开目录」命名歧义 → 「导入目录」（用户报） | p2 | ✅ 已修 | — |
| 4 | [idea] 首页 / 项目菜单「在访达中显示」（用户报） | p2 | ✅ 已修 | — |
| 5 | [bug] 体检完成后健康读数不刷新 | p1 | ✅ 已修 | — |
| 6 | [bug] S2 诊断无行号显示 `Lundefined:undefined` | p2 | ✅ 已修（本地补 S2 一行，随下次发包） | — |
| 7 | [bug] `/favicon.ico` 404 | p2 | ✅ 已修 | — |
| 8 | [bug] 导入的稿没有节点地址，编辑壳里点选不到 | p1 | ⏳ **待拍板**（`11` Q16） | — |
| 9 | [bug] 应用本体没有稿件改名 / 复制 / 移动 / 删除入口 | p1 | ⏳ 待修（下一批第一条） | — |
| 10 | [chore] 壳内自测被 single-instance 静默吞掉 | p2 | ⏳ 待修（先在 `00` §三十八 加 pkill 一行 ✅） | — |
| 11 | [idea] 缩略图只有体检过的稿才有 | p2 | ⏳ **待拍板**（`11` Q17） | — |
| 12 | [idea] 首页同名项目网格视图分不清 | p2 | ⏳ 待修 | — |
| 13 | [chore] 项目页按钮堆满右上角、和 S2 壳重复（用户报） | p1 | ✅ 已修 `58e00c3`（`00` §四十八） | — |
| 14 | [bug] 子目录稿 `@ds` 展开成相对项目根，404、token 全失效 | p1 | ✅ 已修（`00` §五十七；`selftest` 加六条基准） | — |
| 15 | [bug] 属性面板回车落盘发两次 `set_prop`，第二次 400 | p1 | ✅ 已修（`00` §五十七） | — |

