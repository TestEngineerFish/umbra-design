# UmbraDesign · 12 待办清单

> 要什么看 `01-产品需求文档.md`；分期与已定决策看 `11-路线图与分期.md`。
> **这份文件只管条目、状态、验收方式、下一步** —— 不重复需求论证与决策理由。
>
> 状态：`✅ 已完成` · `🚧 在做` · `⬜ 未开始` · `🚫 明确不做`
> 每条都带**验收方式** —— 没有验收方式的条目不算需求，算想法。
>
> 编号稳定，不复用。改了状态就改这里，别另开一份清单。

---

## 〇、进度一览

| 期 | 条目 | 已完成 | 状态 |
| --- | --- | --- | --- |
| **M0** 基础能力 | 24 | **24** | ✅ 全部验收过 |
| **M1** 生命周期 | 14 | 14 | ✅ 全部验收过 |
| **M2** 应用内 AI | 11 | **11** | ✅ 全部完成 |
| **M3** 应用外壳 | 8 | **7** | M3-1 到 M3-6 + M3-8 已完成，M3-7 自动更新可选 |
| **M4** 画布与对比 | 6 | 6 | ✅ 全部验收过 |
| **M5** 打磨 | 7 | **5** | M5-1/2/3/4/5 已完成；M5-6 待 UI 有内容后补 |
| 合计 | **70** | **64（91%）** | M0/M1/M2/M3/M4/M5 基本完成 |

⚠️ **这个百分比只数条目，不代表工作量。** 按工作量估，M2（应用内 AI）
一期大概顶 M1 + M4 两期；M0 那 24 条里也有好几条是几天的量。
真正有意义的读数是下面 §〇.1 那张能力域表。

### 〇.1 按能力域看完成度

| 能力域 | 完成度 | 缺什么 |
| --- | --- | --- |
| 稿件格式 / 运行时 | ✅ 100% | — |
| 写入口与归一化 | ✅ 100% | — |
| 静态校验 | ✅ 100% | — |
| 渲染体检 | ✅ 100% | — |
| 语义版本与四级变更 | ✅ 95% | S6 并排对比（M4-1） |
| 设计系统检索 | ✅ 90% | 编辑能力（M5-2） |
| 界面 · 看 | ✅ 90% | 缩略图、多宽度预览 |
| 界面 · 编辑方式 ① | ✅ 100% | — |
| 编辑方式 ② ③ | ✅ 73% | M2 全部完成（S9 屏 + 通道 A/B + agent 循环 + 选中节点 + 改动可审） |
| **项目生命周期** | ✅ 100% | 引用图谱 + 新建 + 最近项目 + 改配 + 归档删除 + S1/M1-11 UI 入口全部完成 |
| **稿件生命周期** | ✅ 100% | 新建/改名/移动/复制/删除/恢复/文件夹分组 + S8/M1-12 UI 设置屏全部完成 |
| **画布与对比** | ✅ 100% | S6 版本对比 + S10 组件 props 面板 + 多宽度预览 + 深浅色切换 + 缩略图 + 索引过期横幅全部完成 |
| **应用内 AI** | ✅ 73% | M2 全部完成：S9 会话面板 + 通道 A agent 循环（21 工具） + 通道 B Claude Code + 选中节点上下文 + 改动可审 + 用量显示 + agenttest |
| 应用外壳 / 分发 | ✅ 88% | M3-1~6/8 完成；M3-7 自动更新可选 |

### 〇.2 下一步做什么

**M0/M1/M2/M3/M4 已全部完成，M5 仅余 M5-6 待 UI 有内容后补。** 剩余未完成的期如下优先级：

1. **M3 应用外壳**（Tauri 落地、打包、sidecar 启停）
2. **M5 打磨**（搜索、DS 编辑、并发写保护、模板、导出）

---

## 一、M0 · 已完成（基线）

这一节是**已经验收过的能力**，列出来是为了让「剩多少」有分母。
每条后面是它的出处章节。

| # | 事情 | 出处 |
| --- | --- | --- |
| M0-1 | MCP server 骨架（stdio，统一信封 `{ok,data,errors,warnings,stats}`） | `00` §二、§四 |
| M0-2 | 租户与 `@ds` 展开 | `00` §三 |
| M0-3 | 检索类工具（token / 图标 / 组件 / 写稿规则，共 9 个） | `00` §5.1 |
| M0-4 | 唯一写入口 `write_draft` / `patch_draft`（归一化 · `@ds` · `__resources` 注入） | `00` §5.2、§十五 |
| M0-5 | `validate_draft` 静态校验，诊断带 code / 行号 / 洞名 / 改法 | `00` §六 |
| M0-6 | `render_check` 真浏览器体检，判活只认 `1+1` | `00` §七 |
| M0-7 | 语义快照 + 四级 diff + `CHANGELOG-设计侧.md` | `07` |
| M0-8 | 本地静态服务（形态 A）+ `build_index` 入口页 | `00` §十四 |
| M0-9 | 本地 JSON API `/__ud/*`（随机令牌 + Origin 校验） | `00` §二十 |
| M0-10 | 作业注册表（长任务 POST 起 / GET 轮询） | `00` §二十三 |
| M0-11 | 节点地址 `data-ud-node`（内容哈希 + 去重序号） | `09` §二 |
| M0-12 | 逐洞来源审计（字面量 / props / 计算 / 函数 / 认不出） | `00` §十六、`09` §3.1 |
| M0-13 | 属性级写入 `set_prop`（只改一处，是洞就拒绝并说改哪个键） | `09` §3.2 |
| M0-14 | 版本回退 `revert_to`（**向前**的操作，历史只增不改） | `00` §18.3 |
| M0-15 | 样式覆盖通道（拖动中不落盘） | `00` §二十五 |
| M0-16 | 点选桥（注入预览，不进稿） | `00` §二十二 |
| M0-17 | S1 稿件索引（入口页） | `08` S1 |
| M0-18 | S2 单稿预览壳 + **节点属性面板（编辑方式 ①）** | `08` S2、`00` §二十七 |
| M0-19 | S3 诊断面板 | `08` S3 |
| M0-20 | S4 变更清单（四级 + 项目级汇总） | `08` S4、`00` §二十六 |
| M0-21 | S5 设计系统浏览器 | `08` S5 |
| M0-22 | 回归三层：`fixtures/静态` 14 份 · `ui/` 7 份 · 语料（可选）；`fixtures/渲染` 15 份 | `00` §二十八 |
| M0-23 | 交付接入 `ui/_incoming/` + `npm run incoming` | `00` §二十七 |
| M0-24 | 人用入口 `npm run ui`；`build_index` 自维护租户 `.gitignore` | `00` §29.3、§29.4 |

---

## 二、M1 · 项目与稿件生命周期

**这一期的目标**：从一个空目录开始，全程只用界面，走完「建项目 → 建稿 →
改名 → 移动 → 复制 → 删除 → 恢复」，期间 `dc-import` 引用一次都不断。

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M1-0 | **引用图谱**：`list_references(file)` | 回答「谁 import 了我」「我 import 了谁」。全部生命周期操作的前置（`11` §3.3） | 语料里 `Umbra PC 端`（115 个 import）能正确列出双向引用；孤立稿返回空 |
| ✅ M1-1 | `create_project` | 建目录 + `project.json` + `.gitignore` + 可选设计系统骨架 + 第一份稿 | 空目录起步建成，立刻 `get_project` 能读、`npm run ui` 能开 |
| ✅ M1-2 | 工作区与最近项目 | 项目可在任意路径；`projects/` 降级为默认新建位置（依赖 Q5） | 在三个不同路径各建一个项目，最近列表按时间排；删掉目录后列表里标「找不到」而不是崩 |
| ✅ M1-3 | `create_draft` | 四种来源：空白骨架 / 模板 / 复制现有稿 / 把组件包一层 | 四种各建一份，全部 `validate_draft` 零 error、`render_check` 活着 |
| ✅ M1-4 | `rename_draft` | **连带改所有引用它的 `dc-import name`** | 改一个被 3 份稿引用的组件名，改完三份都仍解析得开（`E_IMPORT_MISSING` 为 0） |
| ✅ M1-5 | `duplicate_draft` | 复制含快照要不要跟着复制（我的建议：不跟，新稿从 v1 起） | 复制后两份互不影响，改一份另一份不变 |
| ✅ M1-6 | `move_draft` | 跨目录移动，引用路径要跟着修（相对路径基准变了） | 把一份被跨目录引用的组件移进子目录，引用仍解析得开 |
| ✅ M1-7 | `delete_draft` | 回收站语义（依赖 Q4）；删前列出受影响的引用方并要求确认 | 删一个被引用的组件要先告警；删完能从回收站恢复，恢复后引用恢复 |
| ✅ M1-8 | 文件夹分组 | 建 / 改名 / 移动目录（依赖 Q9） | 建两级目录并把稿分进去，S1 能按目录分组显示 |
| ✅ M1-9 | `update_project` / 项目设置 | 改设计系统路径、限额、标题 | 改完 `get_project` 读到新值，元素数限额立刻生效于下一次校验 |
| ✅ M1-10 | `delete_project` / 归档 | 归档 = 打包成一个目录挪走；删除要二次确认 | 归档后项目消失于列表、目录仍在；删除后目录进回收站 |
| ✅ M1-11 | S1 上的生命周期入口 | 新建 / 改名 / 复制 / 移动 / 删除 / 回收站 / 项目切换器 | 上面每条都能在界面上点到，不用命令行 |
| ✅ M1-12 | 新屏 **S8 项目设置** | 设计系统路径、限额、回收站、危险操作区 | 形制走一轮设计侧；先用我定的一版，不阻塞 |
| ✅ M1-13 | 生命周期回归进 `fixtures/` | 脚本化走一遍全流程，每步后校验 | `npm run lifecycletest` 全绿；故意打断引用能被抓到 |

---

## 三、M2 · 应用内 AI（编辑方式 ②③）

**这一期的目标**：三种编辑方式都在同一个窗口里完成。
大脑走两条通道 —— 依据与合规限制见 `01` §六，架构见 `11` §2.2。

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M2-1 | **通道 A · provider 适配器** | OpenAI Chat Completions 形状；覆盖 DeepSeek 按量、智谱通用 API 按量、任何兼容端点。模型名用户填，**不硬编码** | 同一套代码分别对 DeepSeek 与智谱通用 API 跑通一次带工具调用的对话 |
| ✅ M2-2 | 密钥与端点配置 | 存本地，**不进任何日志、不进 changelog、不随项目走**（密钥属于机器，不属于项目） | 配置改完立刻生效；密钥不出现在任何落盘文件里（grep 验证） |
| ✅ M2-3 | **通道 B · 子进程 Claude Code** | headless 跑，`ANTHROPIC_BASE_URL` + `--mcp-config` + `--allowed-tools`；通过 GLM Coding Plan 的 Anthropic 端点。模型自带 agent 循环，我们只管 spawn + 捕获 JSON 结果 | 应用里发一句话，能看到它调了我们的 MCP 工具并落盘；额度确实走的是订阅 |
| ✅ M2-4 | 两条通道统一到同一接口 | 界面上只是「用哪个大脑」，切换不改会话形状、不改可审口径 | 同一句提问在两条通道下都能完成，改动可审性一致 |
| ✅ M2-5 | 会话模型 | 消息 / 工具调用 / 流式 / 中断；与项目绑定，存 `.umbradesign/chats/` | 关掉应用重开会话还在；中断能真的停下 |
| ✅ M2-6 | agent 循环（仅通道 A 需要） | 21 个工具已接入 `executeToolCall`：get_project、list_drafts、search_tokens、get_token、list_components、get_component、list_icons、get_icon、get_syntax_guide、validate_draft、write_draft、patch_draft、render_check、list_versions、diff_drafts、snapshot_draft、get_changes_since、revert_to、locate_node、set_prop、list_references。模型能连续调 5 个以上工具 | 一次请求里连续调 5 个以上工具能跑完；中途出错有结构化回报 |
| ✅ M2-7 | 新屏 **S9 会话面板** | 嵌在预览壳侧边，不是另开窗口 | 一边看稿一边对话；面板可收起 |
| ✅ M2-8 | **方式 ②**：选中节点带进会话 | chat_send 新增 selectedNodeFile + selectedNodeAddress，调用 locateNode 解析上下文（稿名/地址/标签/源码位置/可编辑项），打包成系统提示。通道 A/B 都使用 | `01` §7.6 第 22 条：点一个元素说「这里字号大一点」→ 只有那一处变 |
| ✅ M2-9 | 改动可审 | `chat_send` 响应中自动包含 `changes` 字段：记录 agent 循环前后各稿版本对比，四级分级（L1-L4）+ 人类可读摘要。通道 A/B 都有 | `01` §7.6 第 23 条 |
| ✅ M2-10 | 用量显示 | 通道 A 返回 `usage`（promptTokens / completionTokens / totalTokens）；通道 B 返回 `usage`（inputTokens / outputTokens / totalCostUSD）+ `toolCalls`（模型调了哪些工具）+ `numTurns` | 一次对话后能看到读数 |
| ✅ M2-11 | `npm run agenttest` | 固定提问 + 固定稿，**不判措辞**，只判三件：改动落点（L2 取值变更 1 处） / 四级分级（counts 字段正确） / 能否回退（颜色恢复） | 全绿；换一次模型仍然全绿（判据不依赖具体模型，直接调业务函数模拟 agent） |

## 四、M3 · 应用外壳与分发

| # | 事情 | 验收 |
| --- | --- | --- |
| ✅ M3-1 | 选型落地（Tauri 2） | `src-tauri/` 下是 Rust 项目，`tauri build` 产出 `.app` + `.dmg`（arm64） |
| ✅ M3-2 | 窗口 / 菜单 / 单实例 / 最近项目 | 单实例聚焦、原生菜单栏、最近项目列表持久化 |
| ✅ M3-3 | server 作为 sidecar 启停 + 端口令牌交接 | 启动自动 spawn 子进程、退出 SIGTERM 清理、三个 Tauri 命令 |
| ✅ M3-4 | 文件夹选择对话框 | 新建 / 打开项目走系统对话框；通过 sidecar 调用 create_project |
| ✅ M3-5 | 打包 `.app` / `.exe`（自带 Node） | 106MB Node 捆绑进 .app；DMG 38MB；干净机器双击能用 |
| ✅ M3-6 | 崩溃恢复与日志 | Session marker 检测非正常退出；on_window_event 清理 sidecar；tauri-plugin-log |
| ✅ M3-8 | 应用前端（项目 → 稿件 → 预览） | `server/dist/ui/index.html` 有实际内容：Welcome 屏 / 项目选择 / 稿件列表 / 筛选搜索 / iframe 预览 / 诊断面板 / 变更记录 / 稿件信息。三个新 Tauri 命令（open_project / close_project / invoke_mcp / get_recent_projects） |
| ⬜ M3-7 | 自动更新（可选） | 能检测到新版本；失败不影响使用 |

---

## 五、M4 · 画布与对比

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M4-1 | 新屏 **S6 版本对比** | 两版并排，滚动同步，变更处高亮。和 S4 是同一份数据的另一种投影 | 对一份改过 3 次的稿，任选两版能并排看出差异，高亮位置和 S4 的行号对得上 |
| ✅ M4-2 | **组件稿 props 面板** | ⚠️ `08` S7 原本定义的是这个（把 `data-props` 里 `editor` 非 null 的项渲染成控件），设计侧交的 S7 是**节点属性面板**，两件事 | 打开一个组件稿，改 props 实时重渲染（走运行时的 `__dcSetProps`），演示态切换钮不混进这个面板 |
| ✅ M4-3 | 多宽度 / 设备预设预览 | S2 已有 zoom 与 preset 的状态位，没接真数据 | 一份稿在 3 个宽度下切换，不重载 iframe |
| ✅ M4-4 | 稿的深浅色切换 | 改的是稿自己的 theme，不是工具皮肤 | 带 `color.dark` 的稿能切换并看出差别 |
| ✅ M4-5 | 缩略图生成 | S1 卡片形态要图；`render_check` 已经能截图 | 建索引时生成，稿改过之后能看出图过期 |
| ✅ M4-6 | S1 索引过期横幅形制 | `doc/10` §五 第 11 项，已在 S1 实现 | 设计侧给形制后替换我定的那一版 |

---

## 六、M5 · 打磨

| # | 事情 | 说明 | 验收 |
| --- | --- | --- | --- |
| ✅ M5-1 | 全局搜索 | 跨稿找 token 用法 / 文案 / 组件引用；四种匹配类型（token/import/css/text） | 搜一个 token 名，列出所有用到它的稿与行号 |
| ✅ M5-2 | 设计系统编辑（依赖 Q7） | set_token_value 只改取值不改结构；改前自动分析影响面 | 改一个 token，事先列出受影响的稿数；改完那些稿的渲染确实变了 |
| ✅ M5-3 | 并发写保护（依赖 Q8） | writeDraft/patchDraft 支持 expectedSourceSha256；不一致拒绝 | 两个客户端同时改同一份稿，后写的那个被拒绝并说清原因 |
| ✅ M5-4 | 稿件模板库 | list_templates / save_as_template / delete_template；create_draft 支持 templateName | 存一份稿为模板，新建时能选到 |
| ✅ M5-5 | 项目导出 / 迁移 | export_project / import_project 用系统 tar；含快照、changelog | 导出再导入到另一台机器，版本历史完整 |
| ⬜ M5-6 | 键盘与可达性过一遍 | 前端 UI 层为空，暂无法实施；等 UI 有内容后补充 | 全程键盘能走完主流程 |
| 🚫 M5-7 | `bundle_draft`（形态 B） | 两点仍未验证，从第一批起就没承诺（`00` §九） | — |

---

## 七、被取代 / 已作废的条目

| 原条目 | 处置 |
| --- | --- |
| 「L2 的最后一环：选中后一颗『复制给模型』按钮」 | **被 M2-6 取代**。有了应用内会话就不需要人肉搬地址了 |
| `doc/10` §三：`decideMeta` / `decideActions` 的内容写进 `任务.dc.html` | **不是我们的事**。那是测试项目里的稿，归项目自己（和用它的 AI）。回归里那 4 条 error 是工具在正常报活 |
| `doc/01` §二「第一阶段调用方是模型，不是人」 | **已过期**，见 `11` §1.2。人是第一使用者 |
| `doc/09` 三层修改路径的 L2/L3 定义 | 仍有效，但 L2 的落点从「人肉转述」变成 M2 的会话 |

---

## 八、决策已全部拍板

Q1–Q10 的结论记在 `11` §四，不在这里重复。摘两条影响条目形状的：

- **Q2**：DeepSeek 直连 + 智谱 GLM 订阅。因订阅限官方工具，落成两条通道 —— 这是 M2 由 9 条变 11 条的原因。
- **Q10**：需求文档整篇重写，冲突内容删除不保留；需求相关三份文件职责互斥（`11` §4.2）。

新的不确定项出现时，登记到 `11` §四下面，不要在本文件里开新的问答区。
