# UmbraDesign · 18 方向调研：目录工作台（2026-09-23）

> **这份文件只管一件事：**「UmbraDesign 要不要从『只做设计稿的软件』变成『能打开任意目录、
> 按文件类型给不同预览与编辑方式、AI 对话在侧的工作台』」—— 这个方向的**依据**：
> 竞品实况、需求是否真实、落到我们这里要改什么、能不能做。
> 它是依据出处（和 `04` / `05` / `16` 一类），**不是需求**。待拍板项登记在 `11` §4.2.3（Q18–Q24），
> 定了再改 `01` / `12`。本文不动代码。
>
> 依据标注：【资料】= 公开页面（来源见 §八）；【实见】= 本机仓库里看到的；【判断】= 我的推断，单独标。

---

## 一、要回答的问题

用户 2026-09-23 提出的方向，原话概括：

> 不应该把项目做成只能设计的产品，它本质是一个**能打开目录的编辑器**。
> 主结构不变：左侧对话、右侧选中文件的预览；按选中文件的类型给不同的编辑方式；
> 布局用动态流式，**优先以内容预览为主**。

拆成三问：

| # | 问题 | 答案在 |
| --- | --- | --- |
| A | 市面上有没有人这么做？做到什么程度？ | §二 |
| B | 这个需求是真实存在，还是只是「看起来合理」？ | §三 |
| C | 落到 UmbraDesign 要改什么、能不能做、代价多大？ | §四 – §六 |

---

## 二、竞品实况

按「和这个方向的距离」分四圈。**圈 1 是同构产品，圈 4 是需求证据**。

### 2.1 圈 1 · 同构：对话在侧 + 本地目录 + 多类型预览【资料】

| 产品 | 时间 | 布局 | 预览什么 | 和我们的关系 |
| --- | --- | --- | --- | --- |
| **Claude Code 桌面版**（改版） | 2026-04-14 | 会话侧栏；「每个面板都能拖放，把终端 / 预览 / diff / 聊天排成你要的网格」；三档显示模式（Verbose / Normal / Summary） | 应用内文件编辑器、diff 查看器、**HTML 与 PDF 在应用内预览**、本地 dev server | 就是「对话 + 目录 + 预览」，但面向开发者，预览类型只到 HTML / PDF |
| **OpenAI Codex app** | 2026-02-02（macOS）/ 03-04（Windows） | 项目 → 线程侧栏；在线程里看 diff、给 diff 加评论；worktree 隔离 | HTML / 图片 / PDF / xlsx / docx | 同上；产物类型更宽 |
| **Google Antigravity** | 2025-11 起 | **两个视图切换**：Editor（看全部代码）/ Agent Manager（只看代理）；Artifacts = 文本 + 图 + 录屏，**可以在截图上圈一块直接给代理留反馈** | 浏览器预览（代理接管时蓝框） | 「按任务切换整套布局」的实例；圈图反馈就是我们方式 ② 的图片版 |
| **Cursor 3.0** | 2026-04-02 | Agents Window 成主界面，IDE 变可选（「可随时切回，或两个都开」）；多聊天并排 / 网格；Design Mode：在浏览器里标注元素发给聊天 | 浏览器预览内嵌 | 编辑器退居次位、对话成主界面，行业方向明确 |
| **「One Claude」** | 2026-09-16 | Cowork + Design + Docs + Slides + Artifacts 并入**同一个对话**；「Claude 自己判断任务需要什么」；Claude Design 单独版「照常可用」 | 文档 / 幻灯片 / 设计 / 网页产物在侧栏 | **参照物本身从『设计专用』变成『对话 + 多种产物类型』**。用户的方向和 Anthropic 一周前的动作是同一个判断 |

一句话：**「左对话、右产物预览、产物类型不限」在 2026 年已经是头部厂商的默认形态**，
差别只在产物存在云端（One Claude）还是本地目录（Claude Code 桌面版 / Codex app）。

### 2.2 圈 2 · 设计专用 + 本地文件【资料】

| 产品 | 形态 | 关键事实 | 和我们的关系 |
| --- | --- | --- | --- |
| **pen.dev**（原 Pencil） | 桌面 App + VS Code / Cursor 扩展 + CLI | 「`.pen` 文件放在应用代码旁边，编码代理可以直接改」；「MCP 设计服务在本地跑，设计文件留在用户机器上」；无限画布 + 图层 + 属性直接操作；目前免费 | **定位重合度最高的一家**：本地文件 + 桌面 App + 本地 MCP 三点全同。差别：它是画布式、私有 `.pen` 格式；我们是文档式、开放 `.dc.html` |
| **Onlook** | 开源（Apache-2.0，26.8k★） | Next.js + Tailwind 项目载入 web container，iframe 预览，DOM ↔ 源码位置映射；三种编辑（可视 / 代码 / AI 聊天）；**已从桌面 App 转向托管版（waitlist）** | 三种编辑方式和我们一致；它放弃桌面版这件事值得记一笔（【判断】原因是 container 依赖和分发成本，不是需求不成立） |
| **Claude Design**（单独版） | 云端 | 见 `16`；2026-09-16 后同时并入对话 | 老参照物 |

### 2.3 圈 3 · 本地知识库 + AI（非代码文件）【资料】

| 产品 | 关键事实 | 说明了什么 |
| --- | --- | --- |
| Obsidian AI 插件群（Copilot / Smart Connections / Smart Composer / Text Generator / Local GPT） | 评测原话：「它们没法把这些上下文给你的其它 AI 工具。这是天花板，而且是结构性的，不是缺个功能」 | 锁在一个 App 里的 AI 是死路；**产物在目录里、任何模型都能接**才是出路（我们的 MCP 出口正是这个） |
| Reor（本地模型笔记 App） | HN 反馈集中在：要能**指向已有目录**、要多格式（txt / PDF）不只 md、**别毁掉 frontmatter**、要多 vault | 「打开任意目录」是硬需求；「工具改文件不能破坏文件」是硬约束（对应我们 H3 / H5） |
| Filer AI（Windows） | 文件管理器 + AI 聊天 + 活动日志 + 右键批处理；「进入子目录，回答的范围跟着缩小 —— 你看到的文件夹就是作用域」；本地优先；免费；macOS 未出 | 「目录即作用域」这条交互规则可以直接借用 |
| Desktop Commander「chat with your notes」 | 无界面，纯靠对话读写 markdown 目录；口号「Markdown 是唯一对 AI 和人都原生的格式」 | 无界面版的同类需求，证明「对话改目录」本身就有人买单 |

### 2.4 圈 4 · 人们的「凑合用法」—— 需求证据的核心【资料】

| 证据 | 内容 | 缺什么 |
| --- | --- | --- |
| 「用 Cursor 打开 Obsidian 库」—— 2025–2026 至少五篇独立文章（web-highlights、matthamlin.me、dev.to、note.com 两篇日文、second-brain.ghost） | 拿到的是**全库上下文 + 语义搜索 + 自然语言问答**；「不需要特殊格式或复杂目录层级」 | 失去**渲染预览、wikilink、内嵌图片**；作者自己也说「要这些就回 Obsidian 插件」 |
| 「谁在做 Cursor for writing」（Medium） | 提出六件套；关键句：「像 Cursor 一样，代理收集建议、就地应用、**让我逐条接受或拒绝，不用离开编辑器**」 | 没有人做全，作者猜要靠 Grammarly / Coda 这类联手 |
| HN「Cursor for Writers」帖 | 存在（本次取页面 429，内容未读到）—— **只作话题热度证据，不作内容依据** | — |
| Claude Code 仓库 issue #95641（Cowork） | 用户抱怨「点文件卡只开侧栏预览，没法用默认应用打开」 | 反过来证明：侧栏预览已成默认形态，用户在它之上提更细的要求 |

---

## 三、需求是否真实 —— 结论

**有依据的结论：**

1. 「对话在侧 + 本地目录 + 按类型预览」的需求**真实存在，而且已被验证**：
   2026 年 2 月（Codex app）、4 月（Cursor 3.0、Claude Code 桌面版）、9 月（One Claude）
   四次头部产品改版全指向同一形态。这不是要不要去验证的事，是**大厂正在收割**的事。
2. 但需求要按人群拆开，三类人的处境完全不同：

| 人群 | 现状 | 缺口 | 我们该不该碰 |
| --- | --- | --- | --- |
| 开发者 | Cursor / Codex app / Claude Code 桌面版已经满足 | 无 | **不碰**。做代码 IDE 必输 |
| 写作 / 笔记（md 目录） | 两头夹着：凑合用 Cursor（没预览）或 Obsidian 插件（锁在 App 里） | 真实：**要预览 + 要 AI + 不要被锁** | 可以顺手覆盖（md 是最便宜的类型），但付费意愿弱（这一圈全免费） |
| 设计 | Claude Design（云）/ pen.dev（本地画布）/ Onlook（转云） | 本地 + 开放格式 + 语义变更交付，目前只有我们 | **核心不变** |

3. 对 **UmbraDesign 自己的使用者**（`01` §二）来说，目录工作台的价值是具体的，不是抽象的：
   - 实现侧要在同一窗口里看 `.dc.html` 和 `CHANGELOG-设计侧.md`，今天做不到（md 没有预览）；
   - 设计项目目录里**本来就有**非稿文件：`project.json`、`_ds/` 下的 tokens、`uploads/` 里的图、评论 json ——
     今天它们在应用里是不可见的；
   - **秘书**（`umbrapc`）要接的是「用户的任意文件」，不只是设计稿；泛型的目录 / 文件能力是接入前提。

**判断（不是结论）：**
需求成立的是**「工作台」形态**，不成立的是**「通用编辑器」**。
工作台 = 一个目录、所有文件都能看、少数类型能改、AI 对什么都能说；
通用编辑器 = 每种类型都做到专业工具的深度 —— 后者做不到，也没必要，
而且和 `01` §八「不做通用网页编辑器」的精神一致。

---

## 四、落到 UmbraDesign：产品定义怎么变

### 4.1 定义句的变化（若 Q18 采纳）

| | 现在（`01` §一） | 提案 |
| --- | --- | --- |
| 定义 | PC 端**设计软件** | 跑在自己电脑上的**目录工作台**：打开一个目录，左边和 AI 对话，右边看选中的文件；**设计（`.dc.html`）是它最深的一种文件类型** |
| 判据 | 「做一个和 ClaudeDesign 基本一样的软件，最大不同是设计稿存本地」 | 「做一个和 One Claude 里『对话 + 产物』基本一样的工作台，最大不同是**产物在你的目录里，任何模型都能接**」 |
| 不变 | 三种编辑方式、`.dc.html` 格式、唯一写入口、语义 diff、断网、MCP 出口 | 全部不变 —— 这些是设计类型的深度，不是被替换的东西 |

**和 `01` §八 不冲突的地方**：仍不做自由画布、仍不做通用 DOM 编辑、仍不做云同步。
**要改的**：§一 定义、§二 使用者（加「任何要和自己目录里的文件对话的人」）、
§四 加一节「文件类型矩阵」、§八 加一行「不做代码 IDE」。改法按 `11` Q10：整段改，不留旧说法。

### 4.2 文件类型矩阵（预览 × 三种编辑方式 × 可行性）

标注可行性：**能做** = 现有栈直接做；**能做·要 vendor** = 断网约束 H2 要求把库打进 `runtime/`；
**可以级** = 能做但不值得第一批；**不做**。

| 类型 | 预览 | ① 手动改 | ② 选中 → AI | ③ 对话 | 可行性 |
| --- | --- | --- | --- | --- | --- |
| `.dc.html` | S2 编辑壳 | 属性面板 | 节点桥 | ✅ | **已有**（M0–M6） |
| 目录 | 列表 / 缩略图网格（S1 稿件索引泛化到所有文件） | 新建 / 改名 / 移动 / 删除（生命周期已有，泛化到非稿） | 选中若干文件当上下文 | ✅ | **能做**：S1 与生命周期工具泛化 |
| `.md` | 渲染视图 + 源码视图切换 | 源码编辑器（所见即所得是第二步） | 选中段落 → AI | ✅ | **能做·要 vendor**：markdown 渲染器 + 文本编辑器（CodeMirror 6 量级，约 300 KB） |
| `.html`（非 dc） | iframe，走本地 http（H1） | 无（只读） | 点选元素带 `outerHTML` 给 AI | ✅ | **能做**；【判断】点选桥要为非 dc 页面单独注入一版，需验证 |
| 图片 png / jpg / svg / gif / webp | 原生 `<img>`，缩放 | **不做**（不做图像编辑） | **圈一块区域 + 一句话 → AI**（Antigravity 式） | ✅ | **能做**；多模态见 §4.4 |
| 代码 / 文本（ts / js / json / css / yaml / txt） | 只读高亮 | 允许改字面量，**不做 IDE 功能**（无补全 / 终端 / 调试） | 选中行 → AI | ✅ | **能做·要 vendor**（和 md 共用编辑器） |
| `.pdf` | 视图 | 无 | 无（第一批） | 文本抽取后可对话 | **可以级**：macOS WKWebView / Windows WebView2 原生能渲染 PDF，Linux 要 pdf.js（约 2 MB） |
| docx / xlsx | 转 HTML 只读（mammoth / SheetJS） | 无 | 无 | 转文本后可对话 | **可以级** |
| pptx | — | — | — | — | **不做** |
| 其它二进制 | 元数据卡（大小 / 时间 / 类型 / 哪些稿引用了它） | 改名 / 移动 | — | — | **能做** |

### 4.3 布局：动态流式，预览优先

提案规则（R1–R5），供拍板（Q20）：

| # | 规则 | 理由 |
| --- | --- | --- |
| R1 | **预览区永远是最大面板**；其它面板按「当前文件类型需要什么」出现：`.dc.html` 出属性 / 诊断 / 变更，`.md` 出大纲，图片什么都不出 | 用户原话「优先以内容预览为主」；Antigravity「按任务换整套布局」的本地版 |
| R2 | 会话栏可左可右、可**收成一条输入条**；收起时预览吃满 | M6-1 已有开关，只加一档「收成条」 |
| R3 | 第一阶段**不做自由拖拽网格**（Claude Code 桌面版那种）；做「按类型预设 + 可收起 + 记住每种类型上次的收放状态」 | 自由网格的状态持久化 / 恢复 / 边界情况是一大块工作；单人用，预设够 |
| R4 | 切换文件时只换**从属面板**，左栏与预览的分界线不动 | 避免每点一个文件整屏跳 |
| R5 | 窗口宽 < 1100 px 时从属面板改抽屉 | Tauri 窗口可能很小 |

**【判断】前置工程**：现在应用前端是**一个 1,729 行的 vanilla `index.html`**（`server/ui/`，124 KB）【实见】。
再塞 4–6 种预览器和一套动态布局，会过可维护上限。建议在动布局前引入构建（Vite，按文件类型拆模块），
这是工程前置，不是需求，登记为 Q21。

### 4.4 AI 侧的变化

| 项 | 现在 | 变化 |
| --- | --- | --- |
| 会话绑定 | 绑项目 | 不变 |
| 方式 ② 的「选中对象」 | 节点 | 泛化为：节点 / 文本区间 / 图片区域 / 文件 / 目录 —— 会话里的「已选中药丸」显示类型 |
| 写入 | 唯一写入口 `write_draft`（dc 专用链：归一化 → `@ds` → `__resources` → 节点地址 → 快照 → changelog） | 非 dc 文件**不能走这条链**（链里每一步都是 dc 语义）。要一条泛型写入口：写前 sha256 校验（Q8 已定）+ 简单快照；**语义 diff 只对 `.dc.html`**。版本纪律怎么定见 Q22 |
| MCP 工具 | 27 个，全是 dc 专用【实见】 | 加泛型：`list_files` / `read_file` / `write_file` / `move_file`（复用生命周期）。**这是秘书接入的必要条件**（Q23） |
| 多模态（图片 → AI） | 通道 A 走 OpenAI Chat Completions 形状 | **可以做**【资料】：DeepSeek 2026-08-21 发了 `deepseek-v4-flash-vision-exp`，OpenAI 兼容、支持 base64 / URL 图片、支持工具调用、每图 ≤ 384 token、标「实验性」；智谱视觉模型同样 OpenAI 兼容。模型名本来就由用户配（`01` §六），只需在配置里给通道标一个「支持图片」开关（Q24） |

### 4.5 明确不做（提案，进 `01` §八）

| 项 | 为什么 |
| --- | --- |
| 代码 IDE：补全 / 终端 / 调试 / git 面板 | §三：开发者已被满足，做了必输 |
| 图像 / PDF 编辑 | 只看、只圈、只对话 |
| 自由画布、云同步、多人、Share | 不变（`01` §八） |
| 每种类型做到专业深度 | 工作台不是通用编辑器（§三判断） |

---

## 五、差异化一句话

| 对手 | 它 | 我们 |
| --- | --- | --- |
| One Claude | 产物在 Anthropic 云端，只有 Claude 能改 | 产物在你的目录里，**任何模型**通过 MCP 都能改 |
| Claude Code 桌面版 / Codex app | 为代码而生，预览是附属 | 为**产物**而生，预览是主面板 |
| pen.dev | 画布式，私有 `.pen` | 文档式，开放 `.dc.html` + 四级语义 diff + 变更清单 |
| Obsidian + 插件 | AI 锁在 App 里 | 目录即作用域，AI 可换、可外接 |

---

## 六、风险与代价

| 风险 | 说明 | 对策 |
| --- | --- | --- |
| **范围膨胀** | 每加一种类型 = 预览器 + 选中桥 + 写入路径 三件套 | 每期只加一种类型，每种有验收用例（对照 `01` §七的口径） |
| **前端重构** | 4.3 的【判断】：单文件 vanilla 撑不住 | Q21 先定；不定就别动布局 |
| **写入纪律分叉** | dc 有语义快照，别的文件没有 —— 用户看到两套「版本」口径 | Q22 定一种说法，界面上明示「这类文件只有快照没有语义 diff」 |
| **定位稀释** | 一旦叫「工作台」，会有人拿它和 Cursor 比 | `01` §八 写死「不做代码 IDE」；首页 / README 第一句仍强调设计 |
| **秘书边界** | 秘书是否直接用这个工作台当文件面，决定泛型 MCP 工具的优先级 | Q23 |
| 圈 4 证据偏开发者社区 | 五篇「Cursor + Obsidian」作者都是技术人 | 【判断】我们的使用者本来就是设计侧 / 产品侧 / 实现侧，偏差可接受 |

---

## 七、对分期的影响（只提方向，排期归 `11`）

- 不打断 M6 / 通道 B 收尾。
- 若 Q18 采纳：新开 **M7 工作台**，建议顺序：Q21 构建前置 → 目录视图泛化 → `.md` → 图片（含圈选 → AI）→ 泛型 MCP 工具 → 代码 / html 只读。
- `.pdf` / docx / xlsx 不排进 M7。

---

## 八、来源

**圈 1**
- Claude Code 桌面版改版（2026-04-14）：`https://claude.com/blog/claude-code-desktop-redesign`
- Codex app（2026-02-02）：`https://openai.com/index/introducing-the-codex-app/`
- Antigravity 评测：`https://www.whatplugin.ai/p/i-tested-antigravity-googles-agentic-ide`
- Cursor 3.0 changelog（2026-04-02）：`https://cursor.com/changelog/3-0`
- One Claude（2026-09-16）：`https://claude.com/blog/cowork-is-now-claude`；Engadget 报道：`https://www.engadget.com/2259938/anthropics-claude-can-now-create-editable-documents-for-you-cowork-chat-together/`；Releasebot 时间线：`https://releasebot.io/updates/anthropic/claude`
- Cowork 预览面板 issue：`https://github.com/anthropics/claude-code/issues/95641`

**圈 2**
- pen.dev：`https://www.therundown.ai/tools/pencil`
- Onlook：`https://github.com/onlook-dev/onlook`

**圈 3**
- Obsidian AI 插件评测（「结构性天花板」）：`https://unabyss.com/blog/best-ai-plugins-for-obsidian`
- Reor HN 讨论：`https://news.ycombinator.com/item?id=39372159`
- Filer AI：`https://filer-ai.com/`
- Desktop Commander 用例：`https://desktopcommander.app/use-cases/knowledge-management/`

**圈 4**
- `https://web-highlights.com/blog/turn-obsidian-into-an-ai-powered-second-brain-using-cursor/`
- `https://www.matthamlin.me/2025/february/cursor-and-obsidian`（正文未抓全，只作存在证据）
- `https://dev.to/nixterra/using-cursor-like-a-smart-notebook-45g7`
- `https://note.com/ob_cafe/n/nd382d3610c5c`、`https://note.com/gay_orchid5021/n/n95982d63d056`
- `https://second-brain.ghost.io/cursor-ai-obsidian-vault-a-second-brain-for-managers-and-teams/`
- 「Cursor for writing」：`https://tejuadeyinka.medium.com/whos-building-the-cursor-for-writing-a76459eda016`
- HN「Cursor for Writers」：`https://news.ycombinator.com/item?id=47027297`（429，未读到内容）

**多模态**
- DeepSeek V4-Flash-Vision-Exp（2026-08-21）：`https://api-docs.deepseek.com/news/news260821/`

**本机实见**：`server/ui/index.html`（1,729 行）、`server/src/*.ts`（27 个 MCP 工具全为 dc 专用）、`doc/01` §八、`doc/16`。

---

## 九、补充（2026-09-23 第二轮）：改名 Umbra Studio、重写还是改、多端与技术选型

用户补充的三件事：① 转工作台的另一个理由 —— 和 Umbra 秘书互补，秘书由此获得文档处理能力，以 MCP / 插件形式接入，自家软件兼容性最好；
② 改名 **Umbra Studio**；③ 第一期要支持 **Web + PC**，以桌面 / Web 为主，后面可能做移动端。

### 9.1 现有资产盘点【实见】

| 部分 | 规模 | 价值 |
| --- | --- | --- |
| `server/`（Node + TS） | 40 个模块：唯一写入口链、静态校验、渲染体检、快照 / 四级 diff、生命周期、会话作业化、两条通道、provider 适配 | **核心资产**。依赖只有 MCP SDK / zod / playwright-core |
| `fixtures/` + `selftest` / `rendertest` / `lifecycletest` | 每份基准钉一条犯过的错 | 回归的全部依据；重写即归零 |
| `server/ui/index.html` | 1,729 行 vanilla 单文件 | M3-8 的过渡产物（「前端无设计稿时先按决策落地」），本来就该换 |
| `src-tauri/` | `lib.rs` 约 660 行：起 / 停 Node sidecar、文件对话框、在访达显示 | 薄壳，可换 |
| `ui/` S1–S10 设计稿 | 11 份 `.dc.html` | 前端重写时的形制依据，不作废 |
| 分发 | Tauri 壳 + 捆一份 106 MB 的 node sidecar | 见 9.4 |

关键事实：**浏览器模式（`/__app/`）和 Tauri 壳跑的是同一份前端**，前端只通过本地 HTTP 和核心说话。
也就是说「一个 Web 前端 + 一个 Node 核心 + 一层薄壳」这个形状**已经在了**，它正是 Web + PC 要的形状。

### 9.2 改还是重写 —— 有依据的结论：**改**

重写会丢掉的：`server/` 40 个模块 + 全部回归基准 + `00` 里四十几节实测记录。
重写能得到的：一个干净的前端 —— 而前端本来就要换。
所以：**核心留、前端重写、壳替换或保留、仓库改名，全部在原仓库上做，git 历史不断。**
反过来说：**不要在旧的 vanilla 前端上继续加任何类型**，新类型全部进新前端工程。

### 9.3 多端架构（提案）

```
浏览器 / 桌面壳 ──HTTP + WS──▶ ┌─────────────────────┐
                              │ Studio 核心（Node）     │──▶ 工作区目录（文件 / 快照 / changelog）
秘书 / Claude Code ──MCP stdio│ 同一进程、同一套能力    │
        或 HTTP ─────────────▶└─────────────────────┘
```

| 端 | 核心跑在哪 | 目录在哪 | 说明 |
| --- | --- | --- | --- |
| PC | 本机，壳里 | 本机 | 今天的形态；H2 断网只对它成立 |
| Web | Umbra 的 ECS 上，Python 服务旁边独立进程 | 每个账号一个工作区目录 | 前端同一份；依赖 Umbra 的账号体系（后期必做多账号） |
| 移动 | 同 Web | 同 Web | 先是 Web 版的响应式（R5 抽屉）或 UmbraiOS 内嵌 WebView；**不做原生** |

前提一条：前端里所有「宿主能力」（选目录对话框、在访达 / 资源管理器显示、通知、外部打开）收进一个 **host adapter** 接口，壳和 Web 各实现一份；前端代码里不允许直接出现 Electron / Tauri API。

**Web 版的三个硬问题（判断，要拍板）**

| # | 问题 | 选项 | 判断 |
| --- | --- | --- | --- |
| W1 | 文件在哪 | (a) 服务器工作区 · (b) 浏览器 File System Access API | **(a)**。(b) 只有 Chrome / Edge 支持、没 git、没渲染体检，等于砍掉核心能力 |
| W2 | 服务器上跑用户 HTML 的渲染体检 = 在服务器开浏览器跑不可信内容 | 必须沙箱：独立容器或用户 + 超时 + 禁网 | **能做**，但是一块安全工作，不在第一期 |
| W3 | 账号 | 依赖 Umbra 多账号 | Web 版排在 PC 之后，不阻塞 |

### 9.4 技术选型（提案）

| 层 | 选择 | 理由 | 可行性 |
| --- | --- | --- | --- |
| 前端 | **Vite + React + TS + Tailwind** | 和 `UmbraPC` 完全同栈【实见】：组件、会话面板可以互搬，将来把 Studio 当面板嵌进 UmbraPC 也是同一栈 | 能做 |
| 编辑器 / 预览器 | CodeMirror 6（md / 代码）、markdown-it、iframe（html / dc） | 全部 vendor（H2） | 能做 |
| 核心 | Node + TS，不动 | Q2 已定；MCP SDK、playwright-core 都在 | — |
| 前端 ⇄ 核心 | HTTP + WebSocket（会话流式、文件变更推送） | 今天已是 HTTP；Tauri 壳另走一条 stdio MCP，**两条路合成一条** | 能做 |
| 桌面壳 | **建议由 Tauri 换 Electron**（重开 Q3） | 见下 | 判断 |

**为什么建议换 Electron（判断，不是结论）**：
① 核心是 Node，Tauri 也得捆 106 MB 的 node sidecar，体积优势已经没了；
② Tauri 三平台三种 webview（WKWebView / WebView2 / WebKitGTK），`.dc.html` 渲染与体检的一致性差，Electron 一份 Chromium；
③ 渲染体检可以直接用 Electron 自带的 Chromium（CDP 连本进程），不再要求用户机器另装 Chrome（今天「没 Chrome 整块跳过」）；
④ 与 UmbraPC 同栈，主进程代码可复用。
代价：壳本身约 660 行，重写一天量级；安装包 150–200 MB，与今天 Tauri + sidecar 相比差别不大。
**没实测的两点**：playwright-core 通过 CDP 接 Electron 自带 Chromium 跑 `render_check` 的稳定性；Electron 主进程内直接跑核心（不开子进程）是否影响 MCP stdio 出口。
→ 先让 Claude Code 做半天 spike 验证这两点，再定 Q26；spike 不过就 Tauri 留着。

### 9.5 与秘书的接入形态

| 形态 | 做法 | 期 |
| --- | --- | --- |
| MCP | 秘书（UmbraPC）作为 MCP 客户端连本机 Studio 核心（stdio）；远端连 ECS 上的核心（HTTP 传输） | **第一期** —— 核心已是 MCP server，只差泛型文件工具（Q23） |
| 插件 / 内嵌面板 | Studio 前端作为一个面板嵌进 UmbraPC | 第二期；同栈才可能，这是选 React 的理由之一 |

### 9.6 改名清单

| 项 | 改法 |
| --- | --- |
| GitHub 仓库 | `umbra-design` → `umbra-studio`（GitHub 自动重定向旧地址） |
| 本地目录 | `Geek/UmbraDesign` → `Geek/UmbraStudio` |
| MCP server 名 | `umbradesign` → `umbrastudio`（已配置的客户端要重加一次） |
| 项目配置目录 | `.umbradesign/` → `.umbrastudio/`，打开旧项目时自动迁移一次 |
| 文档 | `01` / `README` / `CLAUDE.md` 按 Q10 整段改，不留旧名 |
| 不改 | `.dc.html` 格式名、`support.js`、`_ds` 约定 |

名字冲突【资料】：GitHub 上有一个同名 `Nocturne-Ai-Labs/Umbra-Studio`（图像 / 视频生成工作台，4 star，MIT）。不构成阻碍，但同类目撞名，将来上商店 / 注册商标时要留意。

### 9.7 建议顺序

改名（半天）→ 新前端工程骨架 + host adapter（把现有十屏能力**平移**，不加新功能，`selftest` / `rendertest` 全过为验收）→ 壳 spike 定 Q26 → 再按 Q19 加类型 → Web 版最后。
