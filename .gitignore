# ── Umbra Studio 工具自身的仓库 ────────────────────────────────
# 这个仓库只追踪「工具」：doc/ + server/ + runtime/。
# 用户的设计项目不在这里追踪 —— 见下面 projects/。

# 用户的设计项目：各自是独立 git 仓库，位置由用户配置（默认 ./projects）。
# 工具不追踪用户的项目，打包分发后这个目录甚至不在安装目录里。
projects/

# 运行时的权威副本【进仓库】：support.js + 两个 React UMD，共 211 KB。
# 刻意 vendor —— 整个项目的前提是断网可用，让安装依赖 npm 可达就本末倒置了。
# runtime/ 下的文件不要加进忽略。

# 分发到各放稿目录的拷贝：由 runtime/ 派生，不进仓库
ui/support.js
ui/react.production.min.js
ui/react-dom.production.min.js
fixtures/**/support.js
fixtures/**/react.production.min.js
fixtures/**/react-dom.production.min.js

# 设计侧交回来的原件落脚处（doc/00 §二十七）。
# 进来的是别人那边的版本，移植完就没用了 —— 不追踪，避免和 ui/ 下的正本混起来。
ui/_incoming/

# 发给设计侧的打包件（npm run outgoing 产出）：每次都能重打，不进仓库
outgoing/

# 设计侧交付的压缩包：解出来就完了，不进仓库
*.zip

# Node
node_modules/
*.tsbuildinfo
dist/
build/
.npm/
npm-debug.log*
pnpm-debug.log*

# Playwright
/test-results/
/playwright-report/
/.playwright/

# 工具运行产物
.umbrastudio/
.umbradesign/   # 旧名，迁移后留着
*.log

# 环境与密钥
.env
.env.*
!.env.example

# 编辑器与系统
.DS_Store
.AppleDouble
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json.example
*.swp
*.bak
*.bak[0-9]
*.orig

# agenttest 的临时项目：每次跑都会建再删，不进仓库
server/test-agent-project/

doc/Umbra定时测试任务模版.txt

# 新前端（M7-2）
app/node_modules/
app/dist/

# 桌面壳（Electron，M9-2）
shell/node_modules/
shell/out/
